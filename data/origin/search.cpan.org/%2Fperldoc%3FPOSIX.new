<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN">
<html>
 <head>
  <link rel="stylesheet" href="http://st.pimg.net/tucs/style.css" type="text/css" />
<style>
.styleswitch {
  text-align: right;
}
</style>


<script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.4.2/jquery.min.js"></script>
<script type="text/javascript" src="http://st.pimg.net/tucs/js/jquery.cookie.js"></script>
<script type="text/javascript" src="http://st.pimg.net/tucs/js/sh_main.min.js"></script>
<script type="text/javascript" src="http://st.pimg.net/tucs/js/sh_perl.min.js"></script>
<script type="text/javascript" src="http://st.pimg.net/tucs/js/jquery.styleswitch.js"></script>

<link rel="stylesheet" href="http://st.pimg.net/tucs/print.css" type="text/css" media="print" />
  <link rel="alternate" type="application/rss+xml" title="RSS 1.0" href="http://search.cpan.org/uploads.rdf" />
  <link rel="search" href="http://st.pimg.net/tucs/opensearch.xml" type="application/opensearchdescription+xml" title="SearchCPAN" />
  <title>POSIX - search.cpan.org</title>
 <script type="text/javascript">
    var _gaq = _gaq || [];
    _gaq.push(['_setAccount', 'UA-3528438-1']);
    _gaq.push(["_setCustomVar",2,"Distribution","perl",3]);
    _gaq.push(["_setCustomVar",5,"Release","perl-5.14.2",3]);
    _gaq.push(["_setCustomVar",3,"Module","POSIX",3]);
    _gaq.push(["_setCustomVar",1,"Author","FLORA",3]);
    _gaq.push(['_trackPageview']);
  </script>
 </head>
 <body id="cpansearch">
<center><div class="logo"><a href="/"><img src="http://st.pimg.net/tucs/img/cpan_banner.png" alt="CPAN"></a></div></center>
<div class="menubar">
 <a href="/">Home</a>
&middot; <a href="/author/">Authors</a>
&middot; <a href="/recent">Recent</a>
&middot; <a href="http://log.perl.org/cpansearch/">News</a>
&middot; <a href="/mirror">Mirrors</a>
&middot; <a href="/faq.html">FAQ</a>
&middot; <a href="/feedback">Feedback</a>
</div>
<form method="get" action="/search" name="f" class="searchbox">
<input type="text" name="query" value="" size="35">
<br>in <select name="mode">
 <option value="all">All</option>
 <option value="module" >Modules</option>
 <option value="dist" >Distributions</option>
 <option value="author" >Authors</option>
</select>&nbsp;<input type="submit" value="CPAN Search">
</form>


 <a name="_top"></a>
  <div class=path>
<div id=permalink class="noprint"><a href="/perldoc?POSIX">permalink</a></div>
  <a href="/~flora/">Florian Ragwitz</a> &gt;
  <a href="/~flora/perl-5.14.2/">perl-5.14.2</a> &gt;
  POSIX
 </div>

<div class="noprint" style="float:right;align:left;width:19ex">
<a href="http://hexten.net/cpan-faces/"><img src="http://www.gravatar.com/avatar/fd2bdaaefe41908133793d1ddf38f75d?r=g&s=80&d=http%3A%2F%2Fst.pimg.net%2Ftucs%2Fimg%2Fwho.png" width=80 height=80 
style="float:right"
/></a>
<br style="clear:both"/>
<p style="text-align:right">Download:<br/> <a href="/CPAN/authors/id/F/FL/FLORA/perl-5.14.2.tar.gz">perl-5.14.2.tar.gz</a></p>
<p style="text-align:right"><a href="http://deps.cpantesters.org/?module=POSIX;perl=latest">Dependencies</a></p>
<p style="text-align:right"><a href="http://www.annocpan.org/~FLORA/perl-5.14.2/ext/POSIX/lib/POSIX.pod">Annotate this POD
(2)
</a></p>
     <p style="text-align:right"><a href="http://www.perl.org/">Website</a></p>

<div style="float:right">

</div>
</div>
  Module Version:  1.24 &nbsp;
<span class="noprint">
  <a href="/src/FLORA/perl-5.14.2/ext/POSIX/lib/POSIX.pm">Source</a> &nbsp;
  Latest&nbsp;Release:&nbsp;<a href="/~abigail/perl/ext/POSIX/lib/POSIX.pod">perl-5.15.9</a>
</span>
<a name="___top"></a>
<div class=pod>
<div class=toc>
<div class='indexgroup'>
<ul   class='indexList indexList1'>
  <li class='indexItem indexItem1'><a href='#NAME'>NAME</a>
  <li class='indexItem indexItem1'><a href='#SYNOPSIS'>SYNOPSIS</a>
  <li class='indexItem indexItem1'><a href='#DESCRIPTION'>DESCRIPTION</a>
  <li class='indexItem indexItem1'><a href='#NOTE'>NOTE</a>
  <li class='indexItem indexItem1'><a href='#CAVEATS'>CAVEATS</a>
  <li class='indexItem indexItem1'><a href='#FUNCTIONS'>FUNCTIONS</a>
  <li class='indexItem indexItem1'><a href='#CLASSES'>CLASSES</a>
  <ul   class='indexList indexList2'>
    <li class='indexItem indexItem2'><a href='#POSIX::SigAction'>POSIX::SigAction</a>
    <li class='indexItem indexItem2'><a href='#POSIX::SigRt'>POSIX::SigRt</a>
    <li class='indexItem indexItem2'><a href='#POSIX::SigSet'>POSIX::SigSet</a>
    <li class='indexItem indexItem2'><a href='#POSIX::Termios'>POSIX::Termios</a>
  </ul>
  <li class='indexItem indexItem1'><a href='#PATHNAME_CONSTANTS'>PATHNAME CONSTANTS</a>
  <li class='indexItem indexItem1'><a href='#POSIX_CONSTANTS'>POSIX CONSTANTS</a>
  <li class='indexItem indexItem1'><a href='#SYSTEM_CONFIGURATION'>SYSTEM CONFIGURATION</a>
  <li class='indexItem indexItem1'><a href='#ERRNO'>ERRNO</a>
  <li class='indexItem indexItem1'><a href='#FCNTL'>FCNTL</a>
  <li class='indexItem indexItem1'><a href='#FLOAT'>FLOAT</a>
  <li class='indexItem indexItem1'><a href='#LIMITS'>LIMITS</a>
  <li class='indexItem indexItem1'><a href='#LOCALE'>LOCALE</a>
  <li class='indexItem indexItem1'><a href='#MATH'>MATH</a>
  <li class='indexItem indexItem1'><a href='#SIGNAL'>SIGNAL</a>
  <li class='indexItem indexItem1'><a href='#STAT'>STAT</a>
  <li class='indexItem indexItem1'><a href='#STDLIB'>STDLIB</a>
  <li class='indexItem indexItem1'><a href='#STDIO'>STDIO</a>
  <li class='indexItem indexItem1'><a href='#TIME'>TIME</a>
  <li class='indexItem indexItem1'><a href='#UNISTD'>UNISTD</a>
  <li class='indexItem indexItem1'><a href='#WAIT'>WAIT</a>
</ul>
</div>
</div>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="NAME"
>NAME <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<p>POSIX - Perl interface to IEEE Std 1003.1</p>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="SYNOPSIS"
>SYNOPSIS <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<pre class="sh_perl">    use POSIX;
    use POSIX qw(setsid);
    use POSIX qw(:errno_h :fcntl_h);

    printf &#34;EINTR is %d\n&#34;, EINTR;

    $sess_id = POSIX::setsid();

    $fd = POSIX::open($path, O_CREAT|O_EXCL|O_WRONLY, 0644);
        # note: that&#39;s a filedescriptor, *NOT* a filehandle</pre>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="DESCRIPTION"
>DESCRIPTION <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<p>The POSIX module permits you to access all (or nearly all) the standard POSIX 1003.1 identifiers. Many of these identifiers have been given Perl-ish interfaces.</p>

<p><i>Everything is exported by default</i> with the exception of any POSIX functions with the same name as a built-in Perl function, such as <code>abs</code>, <code>alarm</code>, <code>rmdir</code>, <code>write</code>, etc.., which will be exported only if you ask for them explicitly. This is an unfortunate backwards compatibility feature. You can stop the exporting by saying <code>use POSIX ()</code> and then use the fully qualified names (ie. <code>POSIX::SEEK_END</code>).</p>

<p>This document gives a condensed list of the features available in the POSIX module. Consult your operating system&#39;s manpages for general information on most features. Consult <a href="/~flora/perl-5.14.2/pod/perlfunc.pod" class="podlinkpod"
>perlfunc</a> for functions which are noted as being identical to Perl&#39;s builtin functions.</p>

<p>The first section describes POSIX functions from the 1003.1 specification. The second section describes some classes for signal objects, TTY objects, and other miscellaneous objects. The remaining sections list various constants and macros in an organization which roughly follows IEEE Std 1003.1b-1993.</p>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="NOTE"
>NOTE <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<p>The POSIX module is probably the most complex Perl module supplied with the standard distribution. It incorporates autoloading, namespace games, and dynamic loading of code that&#39;s in Perl, C, or both. It&#39;s a great source of wisdom.</p>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="CAVEATS"
>CAVEATS <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<p>A few functions are not implemented because they are C specific. If you attempt to call these, they will print a message telling you that they aren&#39;t implemented, and suggest using the Perl equivalent should one exist. For example, trying to access the setjmp() call will elicit the message &#34;setjmp() is C-specific: use eval {} instead&#34;.</p>

<p>Furthermore, some evil vendors will claim 1003.1 compliance, but in fact are not so: they will not pass the PCTS (POSIX Compliance Test Suites). For example, one vendor may not define EDEADLK, or the semantics of the errno values set by open(2) might not be quite right. Perl does not attempt to verify POSIX compliance. That means you can currently successfully say &#34;use POSIX&#34;, and then later in your program you find that your vendor has been lax and there&#39;s no usable ICANON macro after all. This could be construed to be a bug.</p>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="FUNCTIONS"
>FUNCTIONS <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="_exit"
>_exit</a></dt>

<dd>
<p>This is identical to the C function <code>_exit()</code>. It exits the program immediately which means among other things buffered I/O is <b>not</b> flushed.</p>

<p>Note that when using threads and in Linux this is <b>not</b> a good way to exit a thread because in Linux processes and threads are kind of the same thing (Note: while this is the situation in early 2003 there are projects under way to have threads with more POSIXly semantics in Linux). If you want not to return from a thread, detach the thread.</p>

<dt><a name="abort"
>abort</a></dt>

<dd>
<p>This is identical to the C function <code>abort()</code>. It terminates the process with a <code>SIGABRT</code> signal unless caught by a signal handler or if the handler does not return normally (it e.g. does a <code>longjmp</code>).</p>

<dt><a name="abs"
>abs</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>abs()</code> function, returning the absolute value of its numerical argument.</p>

<dt><a name="access"
>access</a></dt>

<dd>
<p>Determines the accessibility of a file.</p>

<pre class="sh_perl">        if( POSIX::access( &#34;/&#34;, &#38;POSIX::R_OK ) ){
                print &#34;have read permission\n&#34;;
        }</pre>

<p>Returns <code>undef</code> on failure. Note: do not use <code>access()</code> for security purposes. Between the <code>access()</code> call and the operation you are preparing for the permissions might change: a classic <i>race condition</i>.</p>

<dt><a name="acos"
>acos</a></dt>

<dd>
<p>This is identical to the C function <code>acos()</code>, returning the arcus cosine of its numerical argument. See also <a href="/perldoc?Math%3A%3ATrig" class="podlinkpod"
>Math::Trig</a>.</p>

<dt><a name="alarm"
>alarm</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>alarm()</code> function, either for arming or disarming the <code>SIGARLM</code> timer.</p>

<dt><a name="asctime"
>asctime</a></dt>

<dd>
<p>This is identical to the C function <code>asctime()</code>. It returns a string of the form</p>

<pre class="sh_perl">        &#34;Fri Jun  2 18:22:13 2000\n\0&#34;</pre>

<p>and it is called thusly</p>

<pre class="sh_perl">        $asctime = asctime($sec, $min, $hour, $mday, $mon, $year,
                           $wday, $yday, $isdst);</pre>

<p>The <code>$mon</code> is zero-based: January equals <code>0</code>. The <code>$year</code> is 1900-based: 2001 equals <code>101</code>. <code>$wday</code> and <code>$yday</code> default to zero (and are usually ignored anyway), and <code>$isdst</code> defaults to -1.</p>

<dt><a name="asin"
>asin</a></dt>

<dd>
<p>This is identical to the C function <code>asin()</code>, returning the arcus sine of its numerical argument. See also <a href="/perldoc?Math%3A%3ATrig" class="podlinkpod"
>Math::Trig</a>.</p>

<dt><a name="assert"
>assert</a></dt>

<dd>
<p>Unimplemented, but you can use <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#die" class="podlinkpod"
>&#34;die&#34; in perlfunc</a> and the <a href="/~flora/perl-5.14.2/lib/Carp.pm" class="podlinkpod"
>Carp</a> module to achieve similar things.</p>

<dt><a name="atan"
>atan</a></dt>

<dd>
<p>This is identical to the C function <code>atan()</code>, returning the arcus tangent of its numerical argument. See also <a href="/perldoc?Math%3A%3ATrig" class="podlinkpod"
>Math::Trig</a>.</p>

<dt><a name="atan2"
>atan2</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>atan2()</code> function, returning the arcus tangent defined by its two numerical arguments, the <i>y</i> coordinate and the <i>x</i> coordinate. See also <a href="/perldoc?Math%3A%3ATrig" class="podlinkpod"
>Math::Trig</a>.</p>

<dt><a name="atexit"
>atexit</a></dt>

<dd>
<p>atexit() is C-specific: use <code>END {}</code> instead, see <a href="/~flora/perl-5.14.2/pod/perlsub.pod" class="podlinkpod"
>perlsub</a>.</p>

<dt><a name="atof"
>atof</a></dt>

<dd>
<p>atof() is C-specific. Perl converts strings to numbers transparently. If you need to force a scalar to a number, add a zero to it.</p>

<dt><a name="atoi"
>atoi</a></dt>

<dd>
<p>atoi() is C-specific. Perl converts strings to numbers transparently. If you need to force a scalar to a number, add a zero to it. If you need to have just the integer part, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#int" class="podlinkpod"
>&#34;int&#34; in perlfunc</a>.</p>

<dt><a name="atol"
>atol</a></dt>

<dd>
<p>atol() is C-specific. Perl converts strings to numbers transparently. If you need to force a scalar to a number, add a zero to it. If you need to have just the integer part, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#int" class="podlinkpod"
>&#34;int&#34; in perlfunc</a>.</p>

<dt><a name="bsearch"
>bsearch</a></dt>

<dd>
<p>bsearch() not supplied. For doing binary search on wordlists, see <a href="/~flora/perl-5.14.2/lib/Search/Dict.pm" class="podlinkpod"
>Search::Dict</a>.</p>

<dt><a name="calloc"
>calloc</a></dt>

<dd>
<p>calloc() is C-specific. Perl does memory management transparently.</p>

<dt><a name="ceil"
>ceil</a></dt>

<dd>
<p>This is identical to the C function <code>ceil()</code>, returning the smallest integer value greater than or equal to the given numerical argument.</p>

<dt><a name="chdir"
>chdir</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>chdir()</code> function, allowing one to change the working (default) directory, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#chdir" class="podlinkpod"
>&#34;chdir&#34; in perlfunc</a>.</p>

<dt><a name="chmod"
>chmod</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>chmod()</code> function, allowing one to change file and directory permissions, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#chmod" class="podlinkpod"
>&#34;chmod&#34; in perlfunc</a>.</p>

<dt><a name="chown"
>chown</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>chown()</code> function, allowing one to change file and directory owners and groups, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#chown" class="podlinkpod"
>&#34;chown&#34; in perlfunc</a>.</p>

<dt><a name="clearerr"
>clearerr</a></dt>

<dd>
<p>Use the method <code>IO::Handle::clearerr()</code> instead, to reset the error state (if any) and EOF state (if any) of the given stream.</p>

<dt><a name="clock"
>clock</a></dt>

<dd>
<p>This is identical to the C function <code>clock()</code>, returning the amount of spent processor time in microseconds.</p>

<dt><a name="close"
>close</a></dt>

<dd>
<p>Close the file. This uses file descriptors such as those obtained by calling <code>POSIX::open</code>.</p>

<pre class="sh_perl">        $fd = POSIX::open( &#34;foo&#34;, &#38;POSIX::O_RDONLY );
        POSIX::close( $fd );</pre>

<p>Returns <code>undef</code> on failure.</p>

<p>See also <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#close" class="podlinkpod"
>&#34;close&#34; in perlfunc</a>.</p>

<dt><a name="closedir"
>closedir</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>closedir()</code> function for closing a directory handle, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#closedir" class="podlinkpod"
>&#34;closedir&#34; in perlfunc</a>.</p>

<dt><a name="cos"
>cos</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>cos()</code> function, for returning the cosine of its numerical argument, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#cos" class="podlinkpod"
>&#34;cos&#34; in perlfunc</a>. See also <a href="/perldoc?Math%3A%3ATrig" class="podlinkpod"
>Math::Trig</a>.</p>

<dt><a name="cosh"
>cosh</a></dt>

<dd>
<p>This is identical to the C function <code>cosh()</code>, for returning the hyperbolic cosine of its numeric argument. See also <a href="/perldoc?Math%3A%3ATrig" class="podlinkpod"
>Math::Trig</a>.</p>

<dt><a name="creat"
>creat</a></dt>

<dd>
<p>Create a new file. This returns a file descriptor like the ones returned by <code>POSIX::open</code>. Use <code>POSIX::close</code> to close the file.</p>

<pre class="sh_perl">        $fd = POSIX::creat( &#34;foo&#34;, 0611 );
        POSIX::close( $fd );</pre>

<p>See also <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#sysopen" class="podlinkpod"
>&#34;sysopen&#34; in perlfunc</a> and its <code>O_CREAT</code> flag.</p>

<dt><a name="ctermid"
>ctermid</a></dt>

<dd>
<p>Generates the path name for the controlling terminal.</p>

<pre class="sh_perl">        $path = POSIX::ctermid();</pre>

<dt><a name="ctime"
>ctime</a></dt>

<dd>
<p>This is identical to the C function <code>ctime()</code> and equivalent to <code>asctime(localtime(...))</code>, see <a href="#asctime" class="podlinkpod"
>&#34;asctime&#34;</a> and <a href="#localtime" class="podlinkpod"
>&#34;localtime&#34;</a>.</p>

<dt><a name="cuserid"
>cuserid</a></dt>

<dd>
<p>Get the login name of the owner of the current process.</p>

<pre class="sh_perl">        $name = POSIX::cuserid();</pre>

<dt><a name="difftime"
>difftime</a></dt>

<dd>
<p>This is identical to the C function <code>difftime()</code>, for returning the time difference (in seconds) between two times (as returned by <code>time()</code>), see <a href="#time" class="podlinkpod"
>&#34;time&#34;</a>.</p>

<dt><a name="div"
>div</a></dt>

<dd>
<p>div() is C-specific, use <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#int" class="podlinkpod"
>&#34;int&#34; in perlfunc</a> on the usual <code>/</code> division and the modulus <code>%</code>.</p>

<dt><a name="dup"
>dup</a></dt>

<dd>
<p>This is similar to the C function <code>dup()</code>, for duplicating a file descriptor.</p>

<p>This uses file descriptors such as those obtained by calling <code>POSIX::open</code>.</p>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="dup2"
>dup2</a></dt>

<dd>
<p>This is similar to the C function <code>dup2()</code>, for duplicating a file descriptor to an another known file descriptor.</p>

<p>This uses file descriptors such as those obtained by calling <code>POSIX::open</code>.</p>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="errno"
>errno</a></dt>

<dd>
<p>Returns the value of errno.</p>

<pre class="sh_perl">        $errno = POSIX::errno();</pre>

<p>This identical to the numerical values of the <code>$!</code>, see <a href="/~flora/perl-5.14.2/pod/perlvar.pod#$ERRNO" class="podlinkpod"
>&#34;$ERRNO&#34; in perlvar</a>.</p>

<dt><a name="execl"
>execl</a></dt>

<dd>
<p>execl() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#exec" class="podlinkpod"
>&#34;exec&#34; in perlfunc</a>.</p>

<dt><a name="execle"
>execle</a></dt>

<dd>
<p>execle() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#exec" class="podlinkpod"
>&#34;exec&#34; in perlfunc</a>.</p>

<dt><a name="execlp"
>execlp</a></dt>

<dd>
<p>execlp() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#exec" class="podlinkpod"
>&#34;exec&#34; in perlfunc</a>.</p>

<dt><a name="execv"
>execv</a></dt>

<dd>
<p>execv() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#exec" class="podlinkpod"
>&#34;exec&#34; in perlfunc</a>.</p>

<dt><a name="execve"
>execve</a></dt>

<dd>
<p>execve() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#exec" class="podlinkpod"
>&#34;exec&#34; in perlfunc</a>.</p>

<dt><a name="execvp"
>execvp</a></dt>

<dd>
<p>execvp() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#exec" class="podlinkpod"
>&#34;exec&#34; in perlfunc</a>.</p>

<dt><a name="exit"
>exit</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>exit()</code> function for exiting the program, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#exit" class="podlinkpod"
>&#34;exit&#34; in perlfunc</a>.</p>

<dt><a name="exp"
>exp</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>exp()</code> function for returning the exponent (<i>e</i>-based) of the numerical argument, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#exp" class="podlinkpod"
>&#34;exp&#34; in perlfunc</a>.</p>

<dt><a name="fabs"
>fabs</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>abs()</code> function for returning the absolute value of the numerical argument, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#abs" class="podlinkpod"
>&#34;abs&#34; in perlfunc</a>.</p>

<dt><a name="fclose"
>fclose</a></dt>

<dd>
<p>Use method <code>IO::Handle::close()</code> instead, or see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#close" class="podlinkpod"
>&#34;close&#34; in perlfunc</a>.</p>

<dt><a name="fcntl"
>fcntl</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>fcntl()</code> function, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#fcntl" class="podlinkpod"
>&#34;fcntl&#34; in perlfunc</a>.</p>

<dt><a name="fdopen"
>fdopen</a></dt>

<dd>
<p>Use method <code>IO::Handle::new_from_fd()</code> instead, or see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#open" class="podlinkpod"
>&#34;open&#34; in perlfunc</a>.</p>

<dt><a name="feof"
>feof</a></dt>

<dd>
<p>Use method <code>IO::Handle::eof()</code> instead, or see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#eof" class="podlinkpod"
>&#34;eof&#34; in perlfunc</a>.</p>

<dt><a name="ferror"
>ferror</a></dt>

<dd>
<p>Use method <code>IO::Handle::error()</code> instead.</p>

<dt><a name="fflush"
>fflush</a></dt>

<dd>
<p>Use method <code>IO::Handle::flush()</code> instead. See also <a href="/~flora/perl-5.14.2/pod/perlvar.pod#$OUTPUT_AUTOFLUSH" class="podlinkpod"
>&#34;$OUTPUT_AUTOFLUSH&#34; in perlvar</a>.</p>

<dt><a name="fgetc"
>fgetc</a></dt>

<dd>
<p>Use method <code>IO::Handle::getc()</code> instead, or see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#read" class="podlinkpod"
>&#34;read&#34; in perlfunc</a>.</p>

<dt><a name="fgetpos"
>fgetpos</a></dt>

<dd>
<p>Use method <code>IO::Seekable::getpos()</code> instead, or see <a href="/perldoc?L#seek" class="podlinkpod"
>&#34;seek&#34; in L</a>.</p>

<dt><a name="fgets"
>fgets</a></dt>

<dd>
<p>Use method <code>IO::Handle::gets()</code> instead. Similar to &#60;&#62;, also known as <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#readline" class="podlinkpod"
>&#34;readline&#34; in perlfunc</a>.</p>

<dt><a name="fileno"
>fileno</a></dt>

<dd>
<p>Use method <code>IO::Handle::fileno()</code> instead, or see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#fileno" class="podlinkpod"
>&#34;fileno&#34; in perlfunc</a>.</p>

<dt><a name="floor"
>floor</a></dt>

<dd>
<p>This is identical to the C function <code>floor()</code>, returning the largest integer value less than or equal to the numerical argument.</p>

<dt><a name="fmod"
>fmod</a></dt>

<dd>
<p>This is identical to the C function <code>fmod()</code>.</p>

<pre class="sh_perl">        $r = fmod($x, $y);</pre>

<p>It returns the remainder <code>$r = $x - $n*$y</code>, where <code>$n = trunc($x/$y)</code>. The <code>$r</code> has the same sign as <code>$x</code> and magnitude (absolute value) less than the magnitude of <code>$y</code>.</p>

<dt><a name="fopen"
>fopen</a></dt>

<dd>
<p>Use method <code>IO::File::open()</code> instead, or see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#open" class="podlinkpod"
>&#34;open&#34; in perlfunc</a>.</p>

<dt><a name="fork"
>fork</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>fork()</code> function for duplicating the current process, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#fork" class="podlinkpod"
>&#34;fork&#34; in perlfunc</a> and <a href="/~flora/perl-5.14.2/pod/perlfork.pod" class="podlinkpod"
>perlfork</a> if you are in Windows.</p>

<dt><a name="fpathconf"
>fpathconf</a></dt>

<dd>
<p>Retrieves the value of a configurable limit on a file or directory. This uses file descriptors such as those obtained by calling <code>POSIX::open</code>.</p>

<p>The following will determine the maximum length of the longest allowable pathname on the filesystem which holds <code>/var/foo</code>.</p>

<pre class="sh_perl">        $fd = POSIX::open( &#34;/var/foo&#34;, &#38;POSIX::O_RDONLY );
        $path_max = POSIX::fpathconf( $fd, &#38;POSIX::_PC_PATH_MAX );</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="fprintf"
>fprintf</a></dt>

<dd>
<p>fprintf() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#printf" class="podlinkpod"
>&#34;printf&#34; in perlfunc</a> instead.</p>

<dt><a name="fputc"
>fputc</a></dt>

<dd>
<p>fputc() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#print" class="podlinkpod"
>&#34;print&#34; in perlfunc</a> instead.</p>

<dt><a name="fputs"
>fputs</a></dt>

<dd>
<p>fputs() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#print" class="podlinkpod"
>&#34;print&#34; in perlfunc</a> instead.</p>

<dt><a name="fread"
>fread</a></dt>

<dd>
<p>fread() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#read" class="podlinkpod"
>&#34;read&#34; in perlfunc</a> instead.</p>

<dt><a name="free"
>free</a></dt>

<dd>
<p>free() is C-specific. Perl does memory management transparently.</p>

<dt><a name="freopen"
>freopen</a></dt>

<dd>
<p>freopen() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#open" class="podlinkpod"
>&#34;open&#34; in perlfunc</a> instead.</p>

<dt><a name="frexp"
>frexp</a></dt>

<dd>
<p>Return the mantissa and exponent of a floating-point number.</p>

<pre class="sh_perl">        ($mantissa, $exponent) = POSIX::frexp( 1.234e56 );</pre>

<dt><a name="fscanf"
>fscanf</a></dt>

<dd>
<p>fscanf() is C-specific, use &#60;&#62; and regular expressions instead.</p>

<dt><a name="fseek"
>fseek</a></dt>

<dd>
<p>Use method <code>IO::Seekable::seek()</code> instead, or see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#seek" class="podlinkpod"
>&#34;seek&#34; in perlfunc</a>.</p>

<dt><a name="fsetpos"
>fsetpos</a></dt>

<dd>
<p>Use method <code>IO::Seekable::setpos()</code> instead, or seek <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#seek" class="podlinkpod"
>&#34;seek&#34; in perlfunc</a>.</p>

<dt><a name="fstat"
>fstat</a></dt>

<dd>
<p>Get file status. This uses file descriptors such as those obtained by calling <code>POSIX::open</code>. The data returned is identical to the data from Perl&#39;s builtin <code>stat</code> function.</p>

<pre class="sh_perl">        $fd = POSIX::open( &#34;foo&#34;, &#38;POSIX::O_RDONLY );
        @stats = POSIX::fstat( $fd );</pre>

<dt><a name="fsync"
>fsync</a></dt>

<dd>
<p>Use method <code>IO::Handle::sync()</code> instead.</p>

<dt><a name="ftell"
>ftell</a></dt>

<dd>
<p>Use method <code>IO::Seekable::tell()</code> instead, or see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#tell" class="podlinkpod"
>&#34;tell&#34; in perlfunc</a>.</p>

<dt><a name="fwrite"
>fwrite</a></dt>

<dd>
<p>fwrite() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#print" class="podlinkpod"
>&#34;print&#34; in perlfunc</a> instead.</p>

<dt><a name="getc"
>getc</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>getc()</code> function, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#getc" class="podlinkpod"
>&#34;getc&#34; in perlfunc</a>.</p>

<dt><a name="getchar"
>getchar</a></dt>

<dd>
<p>Returns one character from STDIN. Identical to Perl&#39;s <code>getc()</code>, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#getc" class="podlinkpod"
>&#34;getc&#34; in perlfunc</a>.</p>

<dt><a name="getcwd"
>getcwd</a></dt>

<dd>
<p>Returns the name of the current working directory. See also <a href="/perldoc?Cwd" class="podlinkpod"
>Cwd</a>.</p>

<dt><a name="getegid"
>getegid</a></dt>

<dd>
<p>Returns the effective group identifier. Similar to Perl&#39; s builtin variable <code>$(</code>, see <a href="/~flora/perl-5.14.2/pod/perlvar.pod#$EGID" class="podlinkpod"
>&#34;$EGID&#34; in perlvar</a>.</p>

<dt><a name="getenv"
>getenv</a></dt>

<dd>
<p>Returns the value of the specified environment variable. The same information is available through the <code>%ENV</code> array.</p>

<dt><a name="geteuid"
>geteuid</a></dt>

<dd>
<p>Returns the effective user identifier. Identical to Perl&#39;s builtin <code>$&#62;</code> variable, see <a href="/~flora/perl-5.14.2/pod/perlvar.pod#$EUID" class="podlinkpod"
>&#34;$EUID&#34; in perlvar</a>.</p>

<dt><a name="getgid"
>getgid</a></dt>

<dd>
<p>Returns the user&#39;s real group identifier. Similar to Perl&#39;s builtin variable <code>$)</code>, see <a href="/~flora/perl-5.14.2/pod/perlvar.pod#$GID" class="podlinkpod"
>&#34;$GID&#34; in perlvar</a>.</p>

<dt><a name="getgrgid"
>getgrgid</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>getgrgid()</code> function for returning group entries by group identifiers, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#getgrgid" class="podlinkpod"
>&#34;getgrgid&#34; in perlfunc</a>.</p>

<dt><a name="getgrnam"
>getgrnam</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>getgrnam()</code> function for returning group entries by group names, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#getgrnam" class="podlinkpod"
>&#34;getgrnam&#34; in perlfunc</a>.</p>

<dt><a name="getgroups"
>getgroups</a></dt>

<dd>
<p>Returns the ids of the user&#39;s supplementary groups. Similar to Perl&#39;s builtin variable <code>$)</code>, see <a href="/~flora/perl-5.14.2/pod/perlvar.pod#$GID" class="podlinkpod"
>&#34;$GID&#34; in perlvar</a>.</p>

<dt><a name="getlogin"
>getlogin</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>getlogin()</code> function for returning the user name associated with the current session, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#getlogin" class="podlinkpod"
>&#34;getlogin&#34; in perlfunc</a>.</p>

<dt><a name="getpgrp"
>getpgrp</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>getpgrp()</code> function for returning the process group identifier of the current process, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#getpgrp" class="podlinkpod"
>&#34;getpgrp&#34; in perlfunc</a>.</p>

<dt><a name="getpid"
>getpid</a></dt>

<dd>
<p>Returns the process identifier. Identical to Perl&#39;s builtin variable <code>$$</code>, see <a href="/~flora/perl-5.14.2/pod/perlvar.pod#$PID" class="podlinkpod"
>&#34;$PID&#34; in perlvar</a>.</p>

<dt><a name="getppid"
>getppid</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>getppid()</code> function for returning the process identifier of the parent process of the current process , see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#getppid" class="podlinkpod"
>&#34;getppid&#34; in perlfunc</a>.</p>

<dt><a name="getpwnam"
>getpwnam</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>getpwnam()</code> function for returning user entries by user names, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#getpwnam" class="podlinkpod"
>&#34;getpwnam&#34; in perlfunc</a>.</p>

<dt><a name="getpwuid"
>getpwuid</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>getpwuid()</code> function for returning user entries by user identifiers, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#getpwuid" class="podlinkpod"
>&#34;getpwuid&#34; in perlfunc</a>.</p>

<dt><a name="gets"
>gets</a></dt>

<dd>
<p>Returns one line from <code>STDIN</code>, similar to &#60;&#62;, also known as the <code>readline()</code> function, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#readline" class="podlinkpod"
>&#34;readline&#34; in perlfunc</a>.</p>

<p><b>NOTE</b>: if you have C programs that still use <code>gets()</code>, be very afraid. The <code>gets()</code> function is a source of endless grief because it has no buffer overrun checks. It should <b>never</b> be used. The <code>fgets()</code> function should be preferred instead.</p>

<dt><a name="getuid"
>getuid</a></dt>

<dd>
<p>Returns the user&#39;s identifier. Identical to Perl&#39;s builtin <code>$&#60;</code> variable, see <a href="/~flora/perl-5.14.2/pod/perlvar.pod#$UID" class="podlinkpod"
>&#34;$UID&#34; in perlvar</a>.</p>

<dt><a name="gmtime"
>gmtime</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>gmtime()</code> function for converting seconds since the epoch to a date in Greenwich Mean Time, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#gmtime" class="podlinkpod"
>&#34;gmtime&#34; in perlfunc</a>.</p>

<dt><a name="isalnum"
>isalnum</a></dt>

<dd>
<p>This is identical to the C function, except that it can apply to a single character or to a whole string. Note that locale settings may affect what characters are considered <code>isalnum</code>. Does not work on Unicode characters code point 256 or higher. Consider using regular expressions and the <code>/[[:alnum:]]/</code> construct instead, or possibly the <code>/\w/</code> construct.</p>

<dt><a name="isalpha"
>isalpha</a></dt>

<dd>
<p>This is identical to the C function, except that it can apply to a single character or to a whole string. Note that locale settings may affect what characters are considered <code>isalpha</code>. Does not work on Unicode characters code point 256 or higher. Consider using regular expressions and the <code>/[[:alpha:]]/</code> construct instead.</p>

<dt><a name="isatty"
>isatty</a></dt>

<dd>
<p>Returns a boolean indicating whether the specified filehandle is connected to a tty. Similar to the <code>-t</code> operator, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#-X" class="podlinkpod"
>&#34;-X&#34; in perlfunc</a>.</p>

<dt><a name="iscntrl"
>iscntrl</a></dt>

<dd>
<p>This is identical to the C function, except that it can apply to a single character or to a whole string. Note that locale settings may affect what characters are considered <code>iscntrl</code>. Does not work on Unicode characters code point 256 or higher. Consider using regular expressions and the <code>/[[:cntrl:]]/</code> construct instead.</p>

<dt><a name="isdigit"
>isdigit</a></dt>

<dd>
<p>This is identical to the C function, except that it can apply to a single character or to a whole string. Note that locale settings may affect what characters are considered <code>isdigit</code> (unlikely, but still possible). Does not work on Unicode characters code point 256 or higher. Consider using regular expressions and the <code>/[[:digit:]]/</code> construct instead, or the <code>/\d/</code> construct.</p>

<dt><a name="isgraph"
>isgraph</a></dt>

<dd>
<p>This is identical to the C function, except that it can apply to a single character or to a whole string. Note that locale settings may affect what characters are considered <code>isgraph</code>. Does not work on Unicode characters code point 256 or higher. Consider using regular expressions and the <code>/[[:graph:]]/</code> construct instead.</p>

<dt><a name="islower"
>islower</a></dt>

<dd>
<p>This is identical to the C function, except that it can apply to a single character or to a whole string. Note that locale settings may affect what characters are considered <code>islower</code>. Does not work on Unicode characters code point 256 or higher. Consider using regular expressions and the <code>/[[:lower:]]/</code> construct instead. Do <b>not</b> use <code>/[a-z]/</code>.</p>

<dt><a name="isprint"
>isprint</a></dt>

<dd>
<p>This is identical to the C function, except that it can apply to a single character or to a whole string. Note that locale settings may affect what characters are considered <code>isprint</code>. Does not work on Unicode characters code point 256 or higher. Consider using regular expressions and the <code>/[[:print:]]/</code> construct instead.</p>

<dt><a name="ispunct"
>ispunct</a></dt>

<dd>
<p>This is identical to the C function, except that it can apply to a single character or to a whole string. Note that locale settings may affect what characters are considered <code>ispunct</code>. Does not work on Unicode characters code point 256 or higher. Consider using regular expressions and the <code>/[[:punct:]]/</code> construct instead.</p>

<dt><a name="isspace"
>isspace</a></dt>

<dd>
<p>This is identical to the C function, except that it can apply to a single character or to a whole string. Note that locale settings may affect what characters are considered <code>isspace</code>. Does not work on Unicode characters code point 256 or higher. Consider using regular expressions and the <code>/[[:space:]]/</code> construct instead, or the <code>/\s/</code> construct. (Note that <code>/\s/</code> and <code>/[[:space:]]/</code> are slightly different in that <code>/[[:space:]]/</code> can normally match a vertical tab, while <code>/\s/</code> does not.)</p>

<dt><a name="isupper"
>isupper</a></dt>

<dd>
<p>This is identical to the C function, except that it can apply to a single character or to a whole string. Note that locale settings may affect what characters are considered <code>isupper</code>. Does not work on Unicode characters code point 256 or higher. Consider using regular expressions and the <code>/[[:upper:]]/</code> construct instead. Do <b>not</b> use <code>/[A-Z]/</code>.</p>

<dt><a name="isxdigit"
>isxdigit</a></dt>

<dd>
<p>This is identical to the C function, except that it can apply to a single character or to a whole string. Note that locale settings may affect what characters are considered <code>isxdigit</code> (unlikely, but still possible). Does not work on Unicode characters code point 256 or higher. Consider using regular expressions and the <code>/[[:xdigit:]]/</code> construct instead, or simply <code>/[0-9a-f]/i</code>.</p>

<dt><a name="kill"
>kill</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>kill()</code> function for sending signals to processes (often to terminate them), see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#kill" class="podlinkpod"
>&#34;kill&#34; in perlfunc</a>.</p>

<dt><a name="labs"
>labs</a></dt>

<dd>
<p>(For returning absolute values of long integers.) labs() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#abs" class="podlinkpod"
>&#34;abs&#34; in perlfunc</a> instead.</p>

<dt><a name="lchown"
>lchown</a></dt>

<dd>
<p>This is identical to the C function, except the order of arguments is consistent with Perl&#39;s builtin <code>chown()</code> with the added restriction of only one path, not an list of paths. Does the same thing as the <code>chown()</code> function but changes the owner of a symbolic link instead of the file the symbolic link points to.</p>

<dt><a name="ldexp"
>ldexp</a></dt>

<dd>
<p>This is identical to the C function <code>ldexp()</code> for multiplying floating point numbers with powers of two.</p>

<pre class="sh_perl">        $x_quadrupled = POSIX::ldexp($x, 2);</pre>

<dt><a name="ldiv"
>ldiv</a></dt>

<dd>
<p>(For computing dividends of long integers.) ldiv() is C-specific, use <code>/</code> and <code>int()</code> instead.</p>

<dt><a name="link"
>link</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>link()</code> function for creating hard links into files, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#link" class="podlinkpod"
>&#34;link&#34; in perlfunc</a>.</p>

<dt><a name="localeconv"
>localeconv</a></dt>

<dd>
<p>Get numeric formatting information. Returns a reference to a hash containing the current locale formatting values.</p>

<p>Here is how to query the database for the <b>de</b> (Deutsch or German) locale.</p>

<pre class="sh_perl">        $loc = POSIX::setlocale( &#38;POSIX::LC_ALL, &#34;de&#34; );
        print &#34;Locale = $loc\n&#34;;
        $lconv = POSIX::localeconv();
        print &#34;decimal_point    = &#34;, $lconv-&#62;{decimal_point},   &#34;\n&#34;;
        print &#34;thousands_sep    = &#34;, $lconv-&#62;{thousands_sep},   &#34;\n&#34;;
        print &#34;grouping = &#34;, $lconv-&#62;{grouping},        &#34;\n&#34;;
        print &#34;int_curr_symbol  = &#34;, $lconv-&#62;{int_curr_symbol}, &#34;\n&#34;;
        print &#34;currency_symbol  = &#34;, $lconv-&#62;{currency_symbol}, &#34;\n&#34;;
        print &#34;mon_decimal_point = &#34;, $lconv-&#62;{mon_decimal_point}, &#34;\n&#34;;
        print &#34;mon_thousands_sep = &#34;, $lconv-&#62;{mon_thousands_sep}, &#34;\n&#34;;
        print &#34;mon_grouping     = &#34;, $lconv-&#62;{mon_grouping},    &#34;\n&#34;;
        print &#34;positive_sign    = &#34;, $lconv-&#62;{positive_sign},   &#34;\n&#34;;
        print &#34;negative_sign    = &#34;, $lconv-&#62;{negative_sign},   &#34;\n&#34;;
        print &#34;int_frac_digits  = &#34;, $lconv-&#62;{int_frac_digits}, &#34;\n&#34;;
        print &#34;frac_digits      = &#34;, $lconv-&#62;{frac_digits},     &#34;\n&#34;;
        print &#34;p_cs_precedes    = &#34;, $lconv-&#62;{p_cs_precedes},   &#34;\n&#34;;
        print &#34;p_sep_by_space   = &#34;, $lconv-&#62;{p_sep_by_space},  &#34;\n&#34;;
        print &#34;n_cs_precedes    = &#34;, $lconv-&#62;{n_cs_precedes},   &#34;\n&#34;;
        print &#34;n_sep_by_space   = &#34;, $lconv-&#62;{n_sep_by_space},  &#34;\n&#34;;
        print &#34;p_sign_posn      = &#34;, $lconv-&#62;{p_sign_posn},     &#34;\n&#34;;
        print &#34;n_sign_posn      = &#34;, $lconv-&#62;{n_sign_posn},     &#34;\n&#34;;</pre>

<dt><a name="localtime"
>localtime</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>localtime()</code> function for converting seconds since the epoch to a date see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#localtime" class="podlinkpod"
>&#34;localtime&#34; in perlfunc</a>.</p>

<dt><a name="log"
>log</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>log()</code> function, returning the natural (<i>e</i>-based) logarithm of the numerical argument, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#log" class="podlinkpod"
>&#34;log&#34; in perlfunc</a>.</p>

<dt><a name="log10"
>log10</a></dt>

<dd>
<p>This is identical to the C function <code>log10()</code>, returning the 10-base logarithm of the numerical argument. You can also use</p>

<pre class="sh_perl">    sub log10 { log($_[0]) / log(10) }</pre>

<p>or</p>

<pre class="sh_perl">    sub log10 { log($_[0]) / 2.30258509299405 }</pre>

<p>or</p>

<pre class="sh_perl">    sub log10 { log($_[0]) * 0.434294481903252 }</pre>

<dt><a name="longjmp"
>longjmp</a></dt>

<dd>
<p>longjmp() is C-specific: use <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#die" class="podlinkpod"
>&#34;die&#34; in perlfunc</a> instead.</p>

<dt><a name="lseek"
>lseek</a></dt>

<dd>
<p>Move the file&#39;s read/write position. This uses file descriptors such as those obtained by calling <code>POSIX::open</code>.</p>

<pre class="sh_perl">        $fd = POSIX::open( &#34;foo&#34;, &#38;POSIX::O_RDONLY );
        $off_t = POSIX::lseek( $fd, 0, &#38;POSIX::SEEK_SET );</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="malloc"
>malloc</a></dt>

<dd>
<p>malloc() is C-specific. Perl does memory management transparently.</p>

<dt><a name="mblen"
>mblen</a></dt>

<dd>
<p>This is identical to the C function <code>mblen()</code>. Perl does not have any support for the wide and multibyte characters of the C standards, so this might be a rather useless function.</p>

<dt><a name="mbstowcs"
>mbstowcs</a></dt>

<dd>
<p>This is identical to the C function <code>mbstowcs()</code>. Perl does not have any support for the wide and multibyte characters of the C standards, so this might be a rather useless function.</p>

<dt><a name="mbtowc"
>mbtowc</a></dt>

<dd>
<p>This is identical to the C function <code>mbtowc()</code>. Perl does not have any support for the wide and multibyte characters of the C standards, so this might be a rather useless function.</p>

<dt><a name="memchr"
>memchr</a></dt>

<dd>
<p>memchr() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#index" class="podlinkpod"
>&#34;index&#34; in perlfunc</a> instead.</p>

<dt><a name="memcmp"
>memcmp</a></dt>

<dd>
<p>memcmp() is C-specific, use <code>eq</code> instead, see <a href="/~flora/perl-5.14.2/pod/perlop.pod" class="podlinkpod"
>perlop</a>.</p>

<dt><a name="memcpy"
>memcpy</a></dt>

<dd>
<p>memcpy() is C-specific, use <code>=</code>, see <a href="/~flora/perl-5.14.2/pod/perlop.pod" class="podlinkpod"
>perlop</a>, or see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#substr" class="podlinkpod"
>&#34;substr&#34; in perlfunc</a>.</p>

<dt><a name="memmove"
>memmove</a></dt>

<dd>
<p>memmove() is C-specific, use <code>=</code>, see <a href="/~flora/perl-5.14.2/pod/perlop.pod" class="podlinkpod"
>perlop</a>, or see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#substr" class="podlinkpod"
>&#34;substr&#34; in perlfunc</a>.</p>

<dt><a name="memset"
>memset</a></dt>

<dd>
<p>memset() is C-specific, use <code>x</code> instead, see <a href="/~flora/perl-5.14.2/pod/perlop.pod" class="podlinkpod"
>perlop</a>.</p>

<dt><a name="mkdir"
>mkdir</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>mkdir()</code> function for creating directories, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#mkdir" class="podlinkpod"
>&#34;mkdir&#34; in perlfunc</a>.</p>

<dt><a name="mkfifo"
>mkfifo</a></dt>

<dd>
<p>This is similar to the C function <code>mkfifo()</code> for creating FIFO special files.</p>

<pre class="sh_perl">        if (mkfifo($path, $mode)) { ....</pre>

<p>Returns <code>undef</code> on failure. The <code>$mode</code> is similar to the mode of <code>mkdir()</code>, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#mkdir" class="podlinkpod"
>&#34;mkdir&#34; in perlfunc</a>, though for <code>mkfifo</code> you <b>must</b> specify the <code>$mode</code>.</p>

<dt><a name="mktime"
>mktime</a></dt>

<dd>
<p>Convert date/time info to a calendar time.</p>

<p>Synopsis:</p>

<pre class="sh_perl">        mktime(sec, min, hour, mday, mon, year, wday = 0, yday = 0, isdst = -1)</pre>

<p>The month (<code>mon</code>), weekday (<code>wday</code>), and yearday (<code>yday</code>) begin at zero. I.e. January is 0, not 1; Sunday is 0, not 1; January 1st is 0, not 1. The year (<code>year</code>) is given in years since 1900. I.e. The year 1995 is 95; the year 2001 is 101. Consult your system&#39;s <code>mktime()</code> manpage for details about these and the other arguments.</p>

<p>Calendar time for December 12, 1995, at 10:30 am.</p>

<pre class="sh_perl">        $time_t = POSIX::mktime( 0, 30, 10, 12, 11, 95 );
        print &#34;Date = &#34;, POSIX::ctime($time_t);</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="modf"
>modf</a></dt>

<dd>
<p>Return the integral and fractional parts of a floating-point number.</p>

<pre class="sh_perl">        ($fractional, $integral) = POSIX::modf( 3.14 );</pre>

<dt><a name="nice"
>nice</a></dt>

<dd>
<p>This is similar to the C function <code>nice()</code>, for changing the scheduling preference of the current process. Positive arguments mean more polite process, negative values more needy process. Normal user processes can only be more polite.</p>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="offsetof"
>offsetof</a></dt>

<dd>
<p>offsetof() is C-specific, you probably want to see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#pack" class="podlinkpod"
>&#34;pack&#34; in perlfunc</a> instead.</p>

<dt><a name="open"
>open</a></dt>

<dd>
<p>Open a file for reading for writing. This returns file descriptors, not Perl filehandles. Use <code>POSIX::close</code> to close the file.</p>

<p>Open a file read-only with mode 0666.</p>

<pre class="sh_perl">        $fd = POSIX::open( &#34;foo&#34; );</pre>

<p>Open a file for read and write.</p>

<pre class="sh_perl">        $fd = POSIX::open( &#34;foo&#34;, &#38;POSIX::O_RDWR );</pre>

<p>Open a file for write, with truncation.</p>

<pre class="sh_perl">        $fd = POSIX::open( &#34;foo&#34;, &#38;POSIX::O_WRONLY | &#38;POSIX::O_TRUNC );</pre>

<p>Create a new file with mode 0640. Set up the file for writing.</p>

<pre class="sh_perl">        $fd = POSIX::open( &#34;foo&#34;, &#38;POSIX::O_CREAT | &#38;POSIX::O_WRONLY, 0640 );</pre>

<p>Returns <code>undef</code> on failure.</p>

<p>See also <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#sysopen" class="podlinkpod"
>&#34;sysopen&#34; in perlfunc</a>.</p>

<dt><a name="opendir"
>opendir</a></dt>

<dd>
<p>Open a directory for reading.</p>

<pre class="sh_perl">        $dir = POSIX::opendir( &#34;/var&#34; );
        @files = POSIX::readdir( $dir );
        POSIX::closedir( $dir );</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="pathconf"
>pathconf</a></dt>

<dd>
<p>Retrieves the value of a configurable limit on a file or directory.</p>

<p>The following will determine the maximum length of the longest allowable pathname on the filesystem which holds <code>/var</code>.</p>

<pre class="sh_perl">        $path_max = POSIX::pathconf( &#34;/var&#34;, &#38;POSIX::_PC_PATH_MAX );</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="pause"
>pause</a></dt>

<dd>
<p>This is similar to the C function <code>pause()</code>, which suspends the execution of the current process until a signal is received.</p>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="perror"
>perror</a></dt>

<dd>
<p>This is identical to the C function <code>perror()</code>, which outputs to the standard error stream the specified message followed by &#34;: &#34; and the current error string. Use the <code>warn()</code> function and the <code>$!</code> variable instead, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#warn" class="podlinkpod"
>&#34;warn&#34; in perlfunc</a> and <a href="/~flora/perl-5.14.2/pod/perlvar.pod#$ERRNO" class="podlinkpod"
>&#34;$ERRNO&#34; in perlvar</a>.</p>

<dt><a name="pipe"
>pipe</a></dt>

<dd>
<p>Create an interprocess channel. This returns file descriptors like those returned by <code>POSIX::open</code>.</p>

<pre class="sh_perl">        my ($read, $write) = POSIX::pipe();
        POSIX::write( $write, &#34;hello&#34;, 5 );
        POSIX::read( $read, $buf, 5 );</pre>

<p>See also <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#pipe" class="podlinkpod"
>&#34;pipe&#34; in perlfunc</a>.</p>

<dt><a name="pow"
>pow</a></dt>

<dd>
<p>Computes <code>$x</code> raised to the power <code>$exponent</code>.</p>

<pre class="sh_perl">        $ret = POSIX::pow( $x, $exponent );</pre>

<p>You can also use the <code>**</code> operator, see <a href="/~flora/perl-5.14.2/pod/perlop.pod" class="podlinkpod"
>perlop</a>.</p>

<dt><a name="printf"
>printf</a></dt>

<dd>
<p>Formats and prints the specified arguments to STDOUT. See also <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#printf" class="podlinkpod"
>&#34;printf&#34; in perlfunc</a>.</p>

<dt><a name="putc"
>putc</a></dt>

<dd>
<p>putc() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#print" class="podlinkpod"
>&#34;print&#34; in perlfunc</a> instead.</p>

<dt><a name="putchar"
>putchar</a></dt>

<dd>
<p>putchar() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#print" class="podlinkpod"
>&#34;print&#34; in perlfunc</a> instead.</p>

<dt><a name="puts"
>puts</a></dt>

<dd>
<p>puts() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#print" class="podlinkpod"
>&#34;print&#34; in perlfunc</a> instead.</p>

<dt><a name="qsort"
>qsort</a></dt>

<dd>
<p>qsort() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#sort" class="podlinkpod"
>&#34;sort&#34; in perlfunc</a> instead.</p>

<dt><a name="raise"
>raise</a></dt>

<dd>
<p>Sends the specified signal to the current process. See also <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#kill" class="podlinkpod"
>&#34;kill&#34; in perlfunc</a> and the <code>$$</code> in <a href="/~flora/perl-5.14.2/pod/perlvar.pod#$PID" class="podlinkpod"
>&#34;$PID&#34; in perlvar</a>.</p>

<dt><a name="rand"
>rand</a></dt>

<dd>
<p><code>rand()</code> is non-portable, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#rand" class="podlinkpod"
>&#34;rand&#34; in perlfunc</a> instead.</p>

<dt><a name="read"
>read</a></dt>

<dd>
<p>Read from a file. This uses file descriptors such as those obtained by calling <code>POSIX::open</code>. If the buffer <code>$buf</code> is not large enough for the read then Perl will extend it to make room for the request.</p>

<pre class="sh_perl">        $fd = POSIX::open( &#34;foo&#34;, &#38;POSIX::O_RDONLY );
        $bytes = POSIX::read( $fd, $buf, 3 );</pre>

<p>Returns <code>undef</code> on failure.</p>

<p>See also <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#sysread" class="podlinkpod"
>&#34;sysread&#34; in perlfunc</a>.</p>

<dt><a name="readdir"
>readdir</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>readdir()</code> function for reading directory entries, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#readdir" class="podlinkpod"
>&#34;readdir&#34; in perlfunc</a>.</p>

<dt><a name="realloc"
>realloc</a></dt>

<dd>
<p>realloc() is C-specific. Perl does memory management transparently.</p>

<dt><a name="remove"
>remove</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>unlink()</code> function for removing files, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#unlink" class="podlinkpod"
>&#34;unlink&#34; in perlfunc</a>.</p>

<dt><a name="rename"
>rename</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>rename()</code> function for renaming files, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#rename" class="podlinkpod"
>&#34;rename&#34; in perlfunc</a>.</p>

<dt><a name="rewind"
>rewind</a></dt>

<dd>
<p>Seeks to the beginning of the file.</p>

<dt><a name="rewinddir"
>rewinddir</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>rewinddir()</code> function for rewinding directory entry streams, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#rewinddir" class="podlinkpod"
>&#34;rewinddir&#34; in perlfunc</a>.</p>

<dt><a name="rmdir"
>rmdir</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>rmdir()</code> function for removing (empty) directories, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#rmdir" class="podlinkpod"
>&#34;rmdir&#34; in perlfunc</a>.</p>

<dt><a name="scanf"
>scanf</a></dt>

<dd>
<p>scanf() is C-specific, use &#60;&#62; and regular expressions instead, see <a href="/~flora/perl-5.14.2/pod/perlre.pod" class="podlinkpod"
>perlre</a>.</p>

<dt><a name="setgid"
>setgid</a></dt>

<dd>
<p>Sets the real group identifier and the effective group identifier for this process. Similar to assigning a value to the Perl&#39;s builtin <code>$)</code> variable, see <a href="/~flora/perl-5.14.2/pod/perlvar.pod#$EGID" class="podlinkpod"
>&#34;$EGID&#34; in perlvar</a>, except that the latter will change only the real user identifier, and that the setgid() uses only a single numeric argument, as opposed to a space-separated list of numbers.</p>

<dt><a name="setjmp"
>setjmp</a></dt>

<dd>
<p><code>setjmp()</code> is C-specific: use <code>eval {}</code> instead, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#eval" class="podlinkpod"
>&#34;eval&#34; in perlfunc</a>.</p>

<dt><a name="setlocale"
>setlocale</a></dt>

<dd>
<p>Modifies and queries program&#39;s locale. The following examples assume</p>

<pre class="sh_perl">        use POSIX qw(setlocale LC_ALL LC_CTYPE);</pre>

<p>has been issued.</p>

<p>The following will set the traditional UNIX system locale behavior (the second argument <code>&#34;C&#34;</code>).</p>

<pre class="sh_perl">        $loc = setlocale( LC_ALL, &#34;C&#34; );</pre>

<p>The following will query the current LC_CTYPE category. (No second argument means &#39;query&#39;.)</p>

<pre class="sh_perl">        $loc = setlocale( LC_CTYPE );</pre>

<p>The following will set the LC_CTYPE behaviour according to the locale environment variables (the second argument <code>&#34;&#34;</code>). Please see your systems <code>setlocale(3)</code> documentation for the locale environment variables&#39; meaning or consult <a href="/~flora/perl-5.14.2/pod/perllocale.pod" class="podlinkpod"
>perllocale</a>.</p>

<pre class="sh_perl">        $loc = setlocale( LC_CTYPE, &#34;&#34; );</pre>

<p>The following will set the LC_COLLATE behaviour to Argentinian Spanish. <b>NOTE</b>: The naming and availability of locales depends on your operating system. Please consult <a href="/~flora/perl-5.14.2/pod/perllocale.pod" class="podlinkpod"
>perllocale</a> for how to find out which locales are available in your system.</p>

<pre class="sh_perl">        $loc = setlocale( LC_COLLATE, &#34;es_AR.ISO8859-1&#34; );</pre>

<dt><a name="setpgid"
>setpgid</a></dt>

<dd>
<p>This is similar to the C function <code>setpgid()</code> for setting the process group identifier of the current process.</p>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="setsid"
>setsid</a></dt>

<dd>
<p>This is identical to the C function <code>setsid()</code> for setting the session identifier of the current process.</p>

<dt><a name="setuid"
>setuid</a></dt>

<dd>
<p>Sets the real user identifier and the effective user identifier for this process. Similar to assigning a value to the Perl&#39;s builtin <code>$&#60;</code> variable, see <a href="/~flora/perl-5.14.2/pod/perlvar.pod#$UID" class="podlinkpod"
>&#34;$UID&#34; in perlvar</a>, except that the latter will change only the real user identifier.</p>

<dt><a name="sigaction"
>sigaction</a></dt>

<dd>
<p>Detailed signal management. This uses <code>POSIX::SigAction</code> objects for the <code>action</code> and <code>oldaction</code> arguments (the oldaction can also be just a hash reference). Consult your system&#39;s <code>sigaction</code> manpage for details, see also <code>POSIX::SigRt</code>.</p>

<p>Synopsis:</p>

<pre class="sh_perl">        sigaction(signal, action, oldaction = 0)</pre>

<p>Returns <code>undef</code> on failure. The <code>signal</code> must be a number (like SIGHUP), not a string (like &#34;SIGHUP&#34;), though Perl does try hard to understand you.</p>

<p>If you use the SA_SIGINFO flag, the signal handler will in addition to the first argument, the signal name, also receive a second argument, a hash reference, inside which are the following keys with the following semantics, as defined by POSIX/SUSv3:</p>

<pre class="sh_perl">    signo       the signal number
    errno       the error number
    code        if this is zero or less, the signal was sent by
                a user process and the uid and pid make sense,
                otherwise the signal was sent by the kernel</pre>

<p>The following are also defined by POSIX/SUSv3, but unfortunately not very widely implemented:</p>

<pre class="sh_perl">    pid         the process id generating the signal
    uid         the uid of the process id generating the signal
    status      exit value or signal for SIGCHLD
    band        band event for SIGPOLL</pre>

<p>A third argument is also passed to the handler, which contains a copy of the raw binary contents of the siginfo structure: if a system has some non-POSIX fields, this third argument is where to unpack() them from.</p>

<p>Note that not all siginfo values make sense simultaneously (some are valid only for certain signals, for example), and not all values make sense from Perl perspective, you should to consult your system&#39;s <code>sigaction</code> and possibly also <code>siginfo</code> documentation.</p>

<dt><a name="siglongjmp"
>siglongjmp</a></dt>

<dd>
<p>siglongjmp() is C-specific: use <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#die" class="podlinkpod"
>&#34;die&#34; in perlfunc</a> instead.</p>

<dt><a name="sigpending"
>sigpending</a></dt>

<dd>
<p>Examine signals that are blocked and pending. This uses <code>POSIX::SigSet</code> objects for the <code>sigset</code> argument. Consult your system&#39;s <code>sigpending</code> manpage for details.</p>

<p>Synopsis:</p>

<pre class="sh_perl">        sigpending(sigset)</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="sigprocmask"
>sigprocmask</a></dt>

<dd>
<p>Change and/or examine calling process&#39;s signal mask. This uses <code>POSIX::SigSet</code> objects for the <code>sigset</code> and <code>oldsigset</code> arguments. Consult your system&#39;s <code>sigprocmask</code> manpage for details.</p>

<p>Synopsis:</p>

<pre class="sh_perl">        sigprocmask(how, sigset, oldsigset = 0)</pre>

<p>Returns <code>undef</code> on failure.</p>

<p>Note that you can&#39;t reliably block or unblock a signal from its own signal handler if you&#39;re using safe signals. Other signals can be blocked or unblocked reliably.</p>

<dt><a name="sigsetjmp"
>sigsetjmp</a></dt>

<dd>
<p><code>sigsetjmp()</code> is C-specific: use <code>eval {}</code> instead, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#eval" class="podlinkpod"
>&#34;eval&#34; in perlfunc</a>.</p>

<dt><a name="sigsuspend"
>sigsuspend</a></dt>

<dd>
<p>Install a signal mask and suspend process until signal arrives. This uses <code>POSIX::SigSet</code> objects for the <code>signal_mask</code> argument. Consult your system&#39;s <code>sigsuspend</code> manpage for details.</p>

<p>Synopsis:</p>

<pre class="sh_perl">        sigsuspend(signal_mask)</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="sin"
>sin</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>sin()</code> function for returning the sine of the numerical argument, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#sin" class="podlinkpod"
>&#34;sin&#34; in perlfunc</a>. See also <a href="/perldoc?Math%3A%3ATrig" class="podlinkpod"
>Math::Trig</a>.</p>

<dt><a name="sinh"
>sinh</a></dt>

<dd>
<p>This is identical to the C function <code>sinh()</code> for returning the hyperbolic sine of the numerical argument. See also <a href="/perldoc?Math%3A%3ATrig" class="podlinkpod"
>Math::Trig</a>.</p>

<dt><a name="sleep"
>sleep</a></dt>

<dd>
<p>This is functionally identical to Perl&#39;s builtin <code>sleep()</code> function for suspending the execution of the current for process for certain number of seconds, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#sleep" class="podlinkpod"
>&#34;sleep&#34; in perlfunc</a>. There is one significant difference, however: <code>POSIX::sleep()</code> returns the number of <b>unslept</b> seconds, while the <code>CORE::sleep()</code> returns the number of slept seconds.</p>

<dt><a name="sprintf"
>sprintf</a></dt>

<dd>
<p>This is similar to Perl&#39;s builtin <code>sprintf()</code> function for returning a string that has the arguments formatted as requested, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#sprintf" class="podlinkpod"
>&#34;sprintf&#34; in perlfunc</a>.</p>

<dt><a name="sqrt"
>sqrt</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>sqrt()</code> function. for returning the square root of the numerical argument, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#sqrt" class="podlinkpod"
>&#34;sqrt&#34; in perlfunc</a>.</p>

<dt><a name="srand"
>srand</a></dt>

<dd>
<p>Give a seed the pseudorandom number generator, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#srand" class="podlinkpod"
>&#34;srand&#34; in perlfunc</a>.</p>

<dt><a name="sscanf"
>sscanf</a></dt>

<dd>
<p>sscanf() is C-specific, use regular expressions instead, see <a href="/~flora/perl-5.14.2/pod/perlre.pod" class="podlinkpod"
>perlre</a>.</p>

<dt><a name="stat"
>stat</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>stat()</code> function for returning information about files and directories.</p>

<dt><a name="strcat"
>strcat</a></dt>

<dd>
<p>strcat() is C-specific, use <code>.=</code> instead, see <a href="/~flora/perl-5.14.2/pod/perlop.pod" class="podlinkpod"
>perlop</a>.</p>

<dt><a name="strchr"
>strchr</a></dt>

<dd>
<p>strchr() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#index" class="podlinkpod"
>&#34;index&#34; in perlfunc</a> instead.</p>

<dt><a name="strcmp"
>strcmp</a></dt>

<dd>
<p>strcmp() is C-specific, use <code>eq</code> or <code>cmp</code> instead, see <a href="/~flora/perl-5.14.2/pod/perlop.pod" class="podlinkpod"
>perlop</a>.</p>

<dt><a name="strcoll"
>strcoll</a></dt>

<dd>
<p>This is identical to the C function <code>strcoll()</code> for collating (comparing) strings transformed using the <code>strxfrm()</code> function. Not really needed since Perl can do this transparently, see <a href="/~flora/perl-5.14.2/pod/perllocale.pod" class="podlinkpod"
>perllocale</a>.</p>

<dt><a name="strcpy"
>strcpy</a></dt>

<dd>
<p>strcpy() is C-specific, use <code>=</code> instead, see <a href="/~flora/perl-5.14.2/pod/perlop.pod" class="podlinkpod"
>perlop</a>.</p>

<dt><a name="strcspn"
>strcspn</a></dt>

<dd>
<p>strcspn() is C-specific, use regular expressions instead, see <a href="/~flora/perl-5.14.2/pod/perlre.pod" class="podlinkpod"
>perlre</a>.</p>

<dt><a name="strerror"
>strerror</a></dt>

<dd>
<p>Returns the error string for the specified errno. Identical to the string form of the <code>$!</code>, see <a href="/~flora/perl-5.14.2/pod/perlvar.pod#$ERRNO" class="podlinkpod"
>&#34;$ERRNO&#34; in perlvar</a>.</p>

<dt><a name="strftime"
>strftime</a></dt>

<dd>
<p>Convert date and time information to string. Returns the string.</p>

<p>Synopsis:</p>

<pre class="sh_perl">        strftime(fmt, sec, min, hour, mday, mon, year, wday = -1, yday = -1, isdst = -1)</pre>

<p>The month (<code>mon</code>), weekday (<code>wday</code>), and yearday (<code>yday</code>) begin at zero. I.e. January is 0, not 1; Sunday is 0, not 1; January 1st is 0, not 1. The year (<code>year</code>) is given in years since 1900. I.e., the year 1995 is 95; the year 2001 is 101. Consult your system&#39;s <code>strftime()</code> manpage for details about these and the other arguments.</p>

<p>If you want your code to be portable, your format (<code>fmt</code>) argument should use only the conversion specifiers defined by the ANSI C standard (C89, to play safe). These are <code>aAbBcdHIjmMpSUwWxXyYZ%</code>. But even then, the <b>results</b> of some of the conversion specifiers are non-portable. For example, the specifiers <code>aAbBcpZ</code> change according to the locale settings of the user, and both how to set locales (the locale names) and what output to expect are non-standard. The specifier <code>c</code> changes according to the timezone settings of the user and the timezone computation rules of the operating system. The <code>Z</code> specifier is notoriously unportable since the names of timezones are non-standard. Sticking to the numeric specifiers is the safest route.</p>

<p>The given arguments are made consistent as though by calling <code>mktime()</code> before calling your system&#39;s <code>strftime()</code> function, except that the <code>isdst</code> value is not affected.</p>

<p>The string for Tuesday, December 12, 1995.</p>

<pre class="sh_perl">        $str = POSIX::strftime( &#34;%A, %B %d, %Y&#34;, 0, 0, 0, 12, 11, 95, 2 );
        print &#34;$str\n&#34;;</pre>

<dt><a name="strlen"
>strlen</a></dt>

<dd>
<p>strlen() is C-specific, use <code>length()</code> instead, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#length" class="podlinkpod"
>&#34;length&#34; in perlfunc</a>.</p>

<dt><a name="strncat"
>strncat</a></dt>

<dd>
<p>strncat() is C-specific, use <code>.=</code> instead, see <a href="/~flora/perl-5.14.2/pod/perlop.pod" class="podlinkpod"
>perlop</a>.</p>

<dt><a name="strncmp"
>strncmp</a></dt>

<dd>
<p>strncmp() is C-specific, use <code>eq</code> instead, see <a href="/~flora/perl-5.14.2/pod/perlop.pod" class="podlinkpod"
>perlop</a>.</p>

<dt><a name="strncpy"
>strncpy</a></dt>

<dd>
<p>strncpy() is C-specific, use <code>=</code> instead, see <a href="/~flora/perl-5.14.2/pod/perlop.pod" class="podlinkpod"
>perlop</a>.</p>

<dt><a name="strpbrk"
>strpbrk</a></dt>

<dd>
<p>strpbrk() is C-specific, use regular expressions instead, see <a href="/~flora/perl-5.14.2/pod/perlre.pod" class="podlinkpod"
>perlre</a>.</p>

<dt><a name="strrchr"
>strrchr</a></dt>

<dd>
<p>strrchr() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#rindex" class="podlinkpod"
>&#34;rindex&#34; in perlfunc</a> instead.</p>

<dt><a name="strspn"
>strspn</a></dt>

<dd>
<p>strspn() is C-specific, use regular expressions instead, see <a href="/~flora/perl-5.14.2/pod/perlre.pod" class="podlinkpod"
>perlre</a>.</p>

<dt><a name="strstr"
>strstr</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>index()</code> function, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#index" class="podlinkpod"
>&#34;index&#34; in perlfunc</a>.</p>

<dt><a name="strtod"
>strtod</a></dt>

<dd>
<p>String to double translation. Returns the parsed number and the number of characters in the unparsed portion of the string. Truly POSIX-compliant systems set $! ($ERRNO) to indicate a translation error, so clear $! before calling strtod. However, non-POSIX systems may not check for overflow, and therefore will never set $!.</p>

<p>strtod should respect any POSIX <i>setlocale()</i> settings.</p>

<p>To parse a string $str as a floating point number use</p>

<pre class="sh_perl">    $! = 0;
    ($num, $n_unparsed) = POSIX::strtod($str);</pre>

<p>The second returned item and $! can be used to check for valid input:</p>

<pre class="sh_perl">    if (($str eq &#39;&#39;) || ($n_unparsed != 0) || $!) {
        die &#34;Non-numeric input $str&#34; . ($! ? &#34;: $!\n&#34; : &#34;\n&#34;);
    }</pre>

<p>When called in a scalar context strtod returns the parsed number.</p>

<dt><a name="strtok"
>strtok</a></dt>

<dd>
<p>strtok() is C-specific, use regular expressions instead, see <a href="/~flora/perl-5.14.2/pod/perlre.pod" class="podlinkpod"
>perlre</a>, or <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#split" class="podlinkpod"
>&#34;split&#34; in perlfunc</a>.</p>

<dt><a name="strtol"
>strtol</a></dt>

<dd>
<p>String to (long) integer translation. Returns the parsed number and the number of characters in the unparsed portion of the string. Truly POSIX-compliant systems set $! ($ERRNO) to indicate a translation error, so clear $! before calling strtol. However, non-POSIX systems may not check for overflow, and therefore will never set $!.</p>

<p>strtol should respect any POSIX <i>setlocale()</i> settings.</p>

<p>To parse a string $str as a number in some base $base use</p>

<pre class="sh_perl">    $! = 0;
    ($num, $n_unparsed) = POSIX::strtol($str, $base);</pre>

<p>The base should be zero or between 2 and 36, inclusive. When the base is zero or omitted strtol will use the string itself to determine the base: a leading &#34;0x&#34; or &#34;0X&#34; means hexadecimal; a leading &#34;0&#34; means octal; any other leading characters mean decimal. Thus, &#34;1234&#34; is parsed as a decimal number, &#34;01234&#34; as an octal number, and &#34;0x1234&#34; as a hexadecimal number.</p>

<p>The second returned item and $! can be used to check for valid input:</p>

<pre class="sh_perl">    if (($str eq &#39;&#39;) || ($n_unparsed != 0) || !$!) {
        die &#34;Non-numeric input $str&#34; . $! ? &#34;: $!\n&#34; : &#34;\n&#34;;
    }</pre>

<p>When called in a scalar context strtol returns the parsed number.</p>

<dt><a name="strtoul"
>strtoul</a></dt>

<dd>
<p>String to unsigned (long) integer translation. strtoul() is identical to strtol() except that strtoul() only parses unsigned integers. See <a href="#strtol" class="podlinkpod"
>&#34;strtol&#34;</a> for details.</p>

<p>Note: Some vendors supply strtod() and strtol() but not strtoul(). Other vendors that do supply strtoul() parse &#34;-1&#34; as a valid value.</p>

<dt><a name="strxfrm"
>strxfrm</a></dt>

<dd>
<p>String transformation. Returns the transformed string.</p>

<pre class="sh_perl">        $dst = POSIX::strxfrm( $src );</pre>

<p>Used in conjunction with the <code>strcoll()</code> function, see <a href="#strcoll" class="podlinkpod"
>&#34;strcoll&#34;</a>.</p>

<p>Not really needed since Perl can do this transparently, see <a href="/~flora/perl-5.14.2/pod/perllocale.pod" class="podlinkpod"
>perllocale</a>.</p>

<dt><a name="sysconf"
>sysconf</a></dt>

<dd>
<p>Retrieves values of system configurable variables.</p>

<p>The following will get the machine&#39;s clock speed.</p>

<pre class="sh_perl">        $clock_ticks = POSIX::sysconf( &#38;POSIX::_SC_CLK_TCK );</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="system"
>system</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>system()</code> function, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#system" class="podlinkpod"
>&#34;system&#34; in perlfunc</a>.</p>

<dt><a name="tan"
>tan</a></dt>

<dd>
<p>This is identical to the C function <code>tan()</code>, returning the tangent of the numerical argument. See also <a href="/perldoc?Math%3A%3ATrig" class="podlinkpod"
>Math::Trig</a>.</p>

<dt><a name="tanh"
>tanh</a></dt>

<dd>
<p>This is identical to the C function <code>tanh()</code>, returning the hyperbolic tangent of the numerical argument. See also <a href="/perldoc?Math%3A%3ATrig" class="podlinkpod"
>Math::Trig</a>.</p>

<dt><a name="tcdrain"
>tcdrain</a></dt>

<dd>
<p>This is similar to the C function <code>tcdrain()</code> for draining the output queue of its argument stream.</p>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="tcflow"
>tcflow</a></dt>

<dd>
<p>This is similar to the C function <code>tcflow()</code> for controlling the flow of its argument stream.</p>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="tcflush"
>tcflush</a></dt>

<dd>
<p>This is similar to the C function <code>tcflush()</code> for flushing the I/O buffers of its argument stream.</p>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="tcgetpgrp"
>tcgetpgrp</a></dt>

<dd>
<p>This is identical to the C function <code>tcgetpgrp()</code> for returning the process group identifier of the foreground process group of the controlling terminal.</p>

<dt><a name="tcsendbreak"
>tcsendbreak</a></dt>

<dd>
<p>This is similar to the C function <code>tcsendbreak()</code> for sending a break on its argument stream.</p>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="tcsetpgrp"
>tcsetpgrp</a></dt>

<dd>
<p>This is similar to the C function <code>tcsetpgrp()</code> for setting the process group identifier of the foreground process group of the controlling terminal.</p>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="time"
>time</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>time()</code> function for returning the number of seconds since the epoch (whatever it is for the system), see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#time" class="podlinkpod"
>&#34;time&#34; in perlfunc</a>.</p>

<dt><a name="times"
>times</a></dt>

<dd>
<p>The times() function returns elapsed realtime since some point in the past (such as system startup), user and system times for this process, and user and system times used by child processes. All times are returned in clock ticks.</p>

<pre class="sh_perl">    ($realtime, $user, $system, $cuser, $csystem) = POSIX::times();</pre>

<p>Note: Perl&#39;s builtin <code>times()</code> function returns four values, measured in seconds.</p>

<dt><a name="tmpfile"
>tmpfile</a></dt>

<dd>
<p>Use method <code>IO::File::new_tmpfile()</code> instead, or see <a href="/perldoc?File%3A%3ATemp" class="podlinkpod"
>File::Temp</a>.</p>

<dt><a name="tmpnam"
>tmpnam</a></dt>

<dd>
<p>Returns a name for a temporary file.</p>

<pre class="sh_perl">        $tmpfile = POSIX::tmpnam();</pre>

<p>For security reasons, which are probably detailed in your system&#39;s documentation for the C library tmpnam() function, this interface should not be used; instead see <a href="/perldoc?File%3A%3ATemp" class="podlinkpod"
>File::Temp</a>.</p>

<dt><a name="tolower"
>tolower</a></dt>

<dd>
<p>This is identical to the C function, except that it can apply to a single character or to a whole string. Consider using the <code>lc()</code> function, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#lc" class="podlinkpod"
>&#34;lc&#34; in perlfunc</a>, or the equivalent <code>\L</code> operator inside doublequotish strings.</p>

<dt><a name="toupper"
>toupper</a></dt>

<dd>
<p>This is identical to the C function, except that it can apply to a single character or to a whole string. Consider using the <code>uc()</code> function, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#uc" class="podlinkpod"
>&#34;uc&#34; in perlfunc</a>, or the equivalent <code>\U</code> operator inside doublequotish strings.</p>

<dt><a name="ttyname"
>ttyname</a></dt>

<dd>
<p>This is identical to the C function <code>ttyname()</code> for returning the name of the current terminal.</p>

<dt><a name="tzname"
>tzname</a></dt>

<dd>
<p>Retrieves the time conversion information from the <code>tzname</code> variable.</p>

<pre class="sh_perl">        POSIX::tzset();
        ($std, $dst) = POSIX::tzname();</pre>

<dt><a name="tzset"
>tzset</a></dt>

<dd>
<p>This is identical to the C function <code>tzset()</code> for setting the current timezone based on the environment variable <code>TZ</code>, to be used by <code>ctime()</code>, <code>localtime()</code>, <code>mktime()</code>, and <code>strftime()</code> functions.</p>

<dt><a name="umask"
>umask</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>umask()</code> function for setting (and querying) the file creation permission mask, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#umask" class="podlinkpod"
>&#34;umask&#34; in perlfunc</a>.</p>

<dt><a name="uname"
>uname</a></dt>

<dd>
<p>Get name of current operating system.</p>

<pre class="sh_perl">        ($sysname, $nodename, $release, $version, $machine) = POSIX::uname();</pre>

<p>Note that the actual meanings of the various fields are not that well standardized, do not expect any great portability. The <code>$sysname</code> might be the name of the operating system, the <code>$nodename</code> might be the name of the host, the <code>$release</code> might be the (major) release number of the operating system, the <code>$version</code> might be the (minor) release number of the operating system, and the <code>$machine</code> might be a hardware identifier. Maybe.</p>

<dt><a name="ungetc"
>ungetc</a></dt>

<dd>
<p>Use method <code>IO::Handle::ungetc()</code> instead.</p>

<dt><a name="unlink"
>unlink</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>unlink()</code> function for removing files, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#unlink" class="podlinkpod"
>&#34;unlink&#34; in perlfunc</a>.</p>

<dt><a name="utime"
>utime</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>utime()</code> function for changing the time stamps of files and directories, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#utime" class="podlinkpod"
>&#34;utime&#34; in perlfunc</a>.</p>

<dt><a name="vfprintf"
>vfprintf</a></dt>

<dd>
<p>vfprintf() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#printf" class="podlinkpod"
>&#34;printf&#34; in perlfunc</a> instead.</p>

<dt><a name="vprintf"
>vprintf</a></dt>

<dd>
<p>vprintf() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#printf" class="podlinkpod"
>&#34;printf&#34; in perlfunc</a> instead.</p>

<dt><a name="vsprintf"
>vsprintf</a></dt>

<dd>
<p>vsprintf() is C-specific, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#sprintf" class="podlinkpod"
>&#34;sprintf&#34; in perlfunc</a> instead.</p>

<dt><a name="wait"
>wait</a></dt>

<dd>
<p>This is identical to Perl&#39;s builtin <code>wait()</code> function, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#wait" class="podlinkpod"
>&#34;wait&#34; in perlfunc</a>.</p>

<dt><a name="waitpid"
>waitpid</a></dt>

<dd>
<p>Wait for a child process to change state. This is identical to Perl&#39;s builtin <code>waitpid()</code> function, see <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#waitpid" class="podlinkpod"
>&#34;waitpid&#34; in perlfunc</a>.</p>

<pre class="sh_perl">        $pid = POSIX::waitpid( -1, POSIX::WNOHANG );
        print &#34;status = &#34;, ($? / 256), &#34;\n&#34;;</pre>

<dt><a name="wcstombs"
>wcstombs</a></dt>

<dd>
<p>This is identical to the C function <code>wcstombs()</code>. Perl does not have any support for the wide and multibyte characters of the C standards, so this might be a rather useless function.</p>

<dt><a name="wctomb"
>wctomb</a></dt>

<dd>
<p>This is identical to the C function <code>wctomb()</code>. Perl does not have any support for the wide and multibyte characters of the C standards, so this might be a rather useless function.</p>

<dt><a name="write"
>write</a></dt>

<dd>
<p>Write to a file. This uses file descriptors such as those obtained by calling <code>POSIX::open</code>.</p>

<pre class="sh_perl">        $fd = POSIX::open( &#34;foo&#34;, &#38;POSIX::O_WRONLY );
        $buf = &#34;hello&#34;;
        $bytes = POSIX::write( $fd, $buf, 5 );</pre>

<p>Returns <code>undef</code> on failure.</p>

<p>See also <a href="/~flora/perl-5.14.2/pod/perlfunc.pod#syswrite" class="podlinkpod"
>&#34;syswrite&#34; in perlfunc</a>.</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="CLASSES"
>CLASSES <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<h2><a class='u' href='#___top' title='click to go to top of document'
name="POSIX::SigAction"
>POSIX::SigAction</a></h2>

<dl>
<dt><a name="new"
>new</a></dt>

<dd>
<p>Creates a new <code>POSIX::SigAction</code> object which corresponds to the C <code>struct sigaction</code>. This object will be destroyed automatically when it is no longer needed. The first parameter is the handler, a sub reference. The second parameter is a <code>POSIX::SigSet</code> object, it defaults to the empty set. The third parameter contains the <code>sa_flags</code>, it defaults to 0.</p>

<pre class="sh_perl">        $sigset = POSIX::SigSet-&#62;new(SIGINT, SIGQUIT);
        $sigaction = POSIX::SigAction-&#62;new( \&#38;handler, $sigset, &#38;POSIX::SA_NOCLDSTOP );</pre>

<p>This <code>POSIX::SigAction</code> object is intended for use with the <code>POSIX::sigaction()</code> function.</p>
</dd>
</dl>

<dl>
<dt><a name="handler"
>handler</a></dt>

<dd>
<dt><a name="mask"
>mask</a></dt>

<dd>
<dt><a name="flags"
>flags</a></dt>

<dd>
<p>accessor functions to get/set the values of a SigAction object.</p>

<pre class="sh_perl">        $sigset = $sigaction-&#62;mask;
        $sigaction-&#62;flags(&#38;POSIX::SA_RESTART);</pre>

<dt><a name="safe"
>safe</a></dt>

<dd>
<p>accessor function for the &#34;safe signals&#34; flag of a SigAction object; see <a href="/~flora/perl-5.14.2/pod/perlipc.pod" class="podlinkpod"
>perlipc</a> for general information on safe (a.k.a. &#34;deferred&#34;) signals. If you wish to handle a signal safely, use this accessor to set the &#34;safe&#34; flag in the <code>POSIX::SigAction</code> object:</p>

<pre class="sh_perl">        $sigaction-&#62;safe(1);</pre>

<p>You may also examine the &#34;safe&#34; flag on the output action object which is filled in when given as the third parameter to <code>POSIX::sigaction()</code>:</p>

<pre class="sh_perl">        sigaction(SIGINT, $new_action, $old_action);
        if ($old_action-&#62;safe) {
            # previous SIGINT handler used safe signals
        }</pre>
</dd>
</dl>

<h2><a class='u' href='#___top' title='click to go to top of document'
name="POSIX::SigRt"
>POSIX::SigRt</a></h2>

<dl>
<dt><a name='SIGRT'></a><a name="%SIGRT"
>%SIGRT</a></dt>

<dd>
<p>A hash of the POSIX realtime signal handlers. It is an extension of the standard %SIG, the $POSIX::SIGRT{SIGRTMIN} is roughly equivalent to $SIG{SIGRTMIN}, but the right POSIX moves (see below) are made with the POSIX::SigSet and POSIX::sigaction instead of accessing the %SIG.</p>

<p>You can set the %POSIX::SIGRT elements to set the POSIX realtime signal handlers, use <code>delete</code> and <code>exists</code> on the elements, and use <code>scalar</code> on the <code>%POSIX::SIGRT</code> to find out how many POSIX realtime signals there are available (SIGRTMAX - SIGRTMIN + 1, the SIGRTMAX is a valid POSIX realtime signal).</p>

<p>Setting the %SIGRT elements is equivalent to calling this:</p>

<pre class="sh_perl">  sub new {
    my ($rtsig, $handler, $flags) = @_;
    my $sigset = POSIX::SigSet($rtsig);
    my $sigact = POSIX::SigAction-&#62;new($handler, $sigset, $flags);
    sigaction($rtsig, $sigact);
  }</pre>

<p>The flags default to zero, if you want something different you can either use <code>local</code> on $POSIX::SigRt::SIGACTION_FLAGS, or you can derive from POSIX::SigRt and define your own <code>new()</code> (the tied hash STORE method of the %SIGRT calls <code>new($rtsig, $handler, $SIGACTION_FLAGS)</code>, where the $rtsig ranges from zero to SIGRTMAX - SIGRTMIN + 1).</p>

<p>Just as with any signal, you can use sigaction($rtsig, undef, $oa) to retrieve the installed signal handler (or, rather, the signal action).</p>

<p><b>NOTE:</b> whether POSIX realtime signals really work in your system, or whether Perl has been compiled so that it works with them, is outside of this discussion.</p>

<dt><a name="SIGRTMIN"
>SIGRTMIN</a></dt>

<dd>
<p>Return the minimum POSIX realtime signal number available, or <code>undef</code> if no POSIX realtime signals are available.</p>

<dt><a name="SIGRTMAX"
>SIGRTMAX</a></dt>

<dd>
<p>Return the maximum POSIX realtime signal number available, or <code>undef</code> if no POSIX realtime signals are available.</p>
</dd>
</dl>

<h2><a class='u' href='#___top' title='click to go to top of document'
name="POSIX::SigSet"
>POSIX::SigSet</a></h2>

<dl>
<dt><a name="new"
>new</a></dt>

<dd>
<p>Create a new SigSet object. This object will be destroyed automatically when it is no longer needed. Arguments may be supplied to initialize the set.</p>

<p>Create an empty set.</p>

<pre class="sh_perl">        $sigset = POSIX::SigSet-&#62;new;</pre>

<p>Create a set with SIGUSR1.</p>

<pre class="sh_perl">        $sigset = POSIX::SigSet-&#62;new( &#38;POSIX::SIGUSR1 );</pre>

<dt><a name="addset"
>addset</a></dt>

<dd>
<p>Add a signal to a SigSet object.</p>

<pre class="sh_perl">        $sigset-&#62;addset( &#38;POSIX::SIGUSR2 );</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="delset"
>delset</a></dt>

<dd>
<p>Remove a signal from the SigSet object.</p>

<pre class="sh_perl">        $sigset-&#62;delset( &#38;POSIX::SIGUSR2 );</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="emptyset"
>emptyset</a></dt>

<dd>
<p>Initialize the SigSet object to be empty.</p>

<pre class="sh_perl">        $sigset-&#62;emptyset();</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="fillset"
>fillset</a></dt>

<dd>
<p>Initialize the SigSet object to include all signals.</p>

<pre class="sh_perl">        $sigset-&#62;fillset();</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="ismember"
>ismember</a></dt>

<dd>
<p>Tests the SigSet object to see if it contains a specific signal.</p>

<pre class="sh_perl">        if( $sigset-&#62;ismember( &#38;POSIX::SIGUSR1 ) ){
                print &#34;contains SIGUSR1\n&#34;;
        }</pre>
</dd>
</dl>

<h2><a class='u' href='#___top' title='click to go to top of document'
name="POSIX::Termios"
>POSIX::Termios</a></h2>

<dl>
<dt><a name="new"
>new</a></dt>

<dd>
<p>Create a new Termios object. This object will be destroyed automatically when it is no longer needed. A Termios object corresponds to the termios C struct. new() mallocs a new one, getattr() fills it from a file descriptor, and setattr() sets a file descriptor&#39;s parameters to match Termios&#39; contents.</p>

<pre class="sh_perl">        $termios = POSIX::Termios-&#62;new;</pre>

<dt><a name="getattr"
>getattr</a></dt>

<dd>
<p>Get terminal control attributes.</p>

<p>Obtain the attributes for stdin.</p>

<pre class="sh_perl">        $termios-&#62;getattr( 0 ) # Recommended for clarity.
        $termios-&#62;getattr()</pre>

<p>Obtain the attributes for stdout.</p>

<pre class="sh_perl">        $termios-&#62;getattr( 1 )</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="getcc"
>getcc</a></dt>

<dd>
<p>Retrieve a value from the c_cc field of a termios object. The c_cc field is an array so an index must be specified.</p>

<pre class="sh_perl">        $c_cc[1] = $termios-&#62;getcc(1);</pre>

<dt><a name="getcflag"
>getcflag</a></dt>

<dd>
<p>Retrieve the c_cflag field of a termios object.</p>

<pre class="sh_perl">        $c_cflag = $termios-&#62;getcflag;</pre>

<dt><a name="getiflag"
>getiflag</a></dt>

<dd>
<p>Retrieve the c_iflag field of a termios object.</p>

<pre class="sh_perl">        $c_iflag = $termios-&#62;getiflag;</pre>

<dt><a name="getispeed"
>getispeed</a></dt>

<dd>
<p>Retrieve the input baud rate.</p>

<pre class="sh_perl">        $ispeed = $termios-&#62;getispeed;</pre>

<dt><a name="getlflag"
>getlflag</a></dt>

<dd>
<p>Retrieve the c_lflag field of a termios object.</p>

<pre class="sh_perl">        $c_lflag = $termios-&#62;getlflag;</pre>

<dt><a name="getoflag"
>getoflag</a></dt>

<dd>
<p>Retrieve the c_oflag field of a termios object.</p>

<pre class="sh_perl">        $c_oflag = $termios-&#62;getoflag;</pre>

<dt><a name="getospeed"
>getospeed</a></dt>

<dd>
<p>Retrieve the output baud rate.</p>

<pre class="sh_perl">        $ospeed = $termios-&#62;getospeed;</pre>

<dt><a name="setattr"
>setattr</a></dt>

<dd>
<p>Set terminal control attributes.</p>

<p>Set attributes immediately for stdout.</p>

<pre class="sh_perl">        $termios-&#62;setattr( 1, &#38;POSIX::TCSANOW );</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="setcc"
>setcc</a></dt>

<dd>
<p>Set a value in the c_cc field of a termios object. The c_cc field is an array so an index must be specified.</p>

<pre class="sh_perl">        $termios-&#62;setcc( &#38;POSIX::VEOF, 1 );</pre>

<dt><a name="setcflag"
>setcflag</a></dt>

<dd>
<p>Set the c_cflag field of a termios object.</p>

<pre class="sh_perl">        $termios-&#62;setcflag( $c_cflag | &#38;POSIX::CLOCAL );</pre>

<dt><a name="setiflag"
>setiflag</a></dt>

<dd>
<p>Set the c_iflag field of a termios object.</p>

<pre class="sh_perl">        $termios-&#62;setiflag( $c_iflag | &#38;POSIX::BRKINT );</pre>

<dt><a name="setispeed"
>setispeed</a></dt>

<dd>
<p>Set the input baud rate.</p>

<pre class="sh_perl">        $termios-&#62;setispeed( &#38;POSIX::B9600 );</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name="setlflag"
>setlflag</a></dt>

<dd>
<p>Set the c_lflag field of a termios object.</p>

<pre class="sh_perl">        $termios-&#62;setlflag( $c_lflag | &#38;POSIX::ECHO );</pre>

<dt><a name="setoflag"
>setoflag</a></dt>

<dd>
<p>Set the c_oflag field of a termios object.</p>

<pre class="sh_perl">        $termios-&#62;setoflag( $c_oflag | &#38;POSIX::OPOST );</pre>

<dt><a name="setospeed"
>setospeed</a></dt>

<dd>
<p>Set the output baud rate.</p>

<pre class="sh_perl">        $termios-&#62;setospeed( &#38;POSIX::B9600 );</pre>

<p>Returns <code>undef</code> on failure.</p>

<dt><a name='Baud'></a><a name="Baud_rate_values"
>Baud rate values</a></dt>

<dd>
<p>B38400 B75 B200 B134 B300 B1800 B150 B0 B19200 B1200 B9600 B600 B4800 B50 B2400 B110</p>

<dt><a name='Terminal'></a><a name="Terminal_interface_values"
>Terminal interface values</a></dt>

<dd>
<p>TCSADRAIN TCSANOW TCOON TCIOFLUSH TCOFLUSH TCION TCIFLUSH TCSAFLUSH TCIOFF TCOOFF</p>

<dt><a name='c_cc'></a><a name="c_cc_field_values"
>c_cc field values</a></dt>

<dd>
<p>VEOF VEOL VERASE VINTR VKILL VQUIT VSUSP VSTART VSTOP VMIN VTIME NCCS</p>

<dt><a name='c_cflag'></a><a name="c_cflag_field_values"
>c_cflag field values</a></dt>

<dd>
<p>CLOCAL CREAD CSIZE CS5 CS6 CS7 CS8 CSTOPB HUPCL PARENB PARODD</p>

<dt><a name='c_iflag'></a><a name="c_iflag_field_values"
>c_iflag field values</a></dt>

<dd>
<p>BRKINT ICRNL IGNBRK IGNCR IGNPAR INLCR INPCK ISTRIP IXOFF IXON PARMRK</p>

<dt><a name='c_lflag'></a><a name="c_lflag_field_values"
>c_lflag field values</a></dt>

<dd>
<p>ECHO ECHOE ECHOK ECHONL ICANON IEXTEN ISIG NOFLSH TOSTOP</p>

<dt><a name='c_oflag'></a><a name="c_oflag_field_values"
>c_oflag field values</a></dt>

<dd>
<p>OPOST</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="PATHNAME_CONSTANTS"
>PATHNAME CONSTANTS <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>_PC_CHOWN_RESTRICTED _PC_LINK_MAX _PC_MAX_CANON _PC_MAX_INPUT _PC_NAME_MAX _PC_NO_TRUNC _PC_PATH_MAX _PC_PIPE_BUF _PC_VDISABLE</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="POSIX_CONSTANTS"
>POSIX CONSTANTS <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>_POSIX_ARG_MAX _POSIX_CHILD_MAX _POSIX_CHOWN_RESTRICTED _POSIX_JOB_CONTROL _POSIX_LINK_MAX _POSIX_MAX_CANON _POSIX_MAX_INPUT _POSIX_NAME_MAX _POSIX_NGROUPS_MAX _POSIX_NO_TRUNC _POSIX_OPEN_MAX _POSIX_PATH_MAX _POSIX_PIPE_BUF _POSIX_SAVED_IDS _POSIX_SSIZE_MAX _POSIX_STREAM_MAX _POSIX_TZNAME_MAX _POSIX_VDISABLE _POSIX_VERSION</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="SYSTEM_CONFIGURATION"
>SYSTEM CONFIGURATION <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>_SC_ARG_MAX _SC_CHILD_MAX _SC_CLK_TCK _SC_JOB_CONTROL _SC_NGROUPS_MAX _SC_OPEN_MAX _SC_PAGESIZE _SC_SAVED_IDS _SC_STREAM_MAX _SC_TZNAME_MAX _SC_VERSION</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="ERRNO"
>ERRNO <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>E2BIG EACCES EADDRINUSE EADDRNOTAVAIL EAFNOSUPPORT EAGAIN EALREADY EBADF EBUSY ECHILD ECONNABORTED ECONNREFUSED ECONNRESET EDEADLK EDESTADDRREQ EDOM EDQUOT EEXIST EFAULT EFBIG EHOSTDOWN EHOSTUNREACH EINPROGRESS EINTR EINVAL EIO EISCONN EISDIR ELOOP EMFILE EMLINK EMSGSIZE ENAMETOOLONG ENETDOWN ENETRESET ENETUNREACH ENFILE ENOBUFS ENODEV ENOENT ENOEXEC ENOLCK ENOMEM ENOPROTOOPT ENOSPC ENOSYS ENOTBLK ENOTCONN ENOTDIR ENOTEMPTY ENOTSOCK ENOTTY ENXIO EOPNOTSUPP EPERM EPFNOSUPPORT EPIPE EPROCLIM EPROTONOSUPPORT EPROTOTYPE ERANGE EREMOTE ERESTART EROFS ESHUTDOWN ESOCKTNOSUPPORT ESPIPE ESRCH ESTALE ETIMEDOUT ETOOMANYREFS ETXTBSY EUSERS EWOULDBLOCK EXDEV</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="FCNTL"
>FCNTL <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>FD_CLOEXEC F_DUPFD F_GETFD F_GETFL F_GETLK F_OK F_RDLCK F_SETFD F_SETFL F_SETLK F_SETLKW F_UNLCK F_WRLCK O_ACCMODE O_APPEND O_CREAT O_EXCL O_NOCTTY O_NONBLOCK O_RDONLY O_RDWR O_TRUNC O_WRONLY</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="FLOAT"
>FLOAT <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>DBL_DIG DBL_EPSILON DBL_MANT_DIG DBL_MAX DBL_MAX_10_EXP DBL_MAX_EXP DBL_MIN DBL_MIN_10_EXP DBL_MIN_EXP FLT_DIG FLT_EPSILON FLT_MANT_DIG FLT_MAX FLT_MAX_10_EXP FLT_MAX_EXP FLT_MIN FLT_MIN_10_EXP FLT_MIN_EXP FLT_RADIX FLT_ROUNDS LDBL_DIG LDBL_EPSILON LDBL_MANT_DIG LDBL_MAX LDBL_MAX_10_EXP LDBL_MAX_EXP LDBL_MIN LDBL_MIN_10_EXP LDBL_MIN_EXP</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="LIMITS"
>LIMITS <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>ARG_MAX CHAR_BIT CHAR_MAX CHAR_MIN CHILD_MAX INT_MAX INT_MIN LINK_MAX LONG_MAX LONG_MIN MAX_CANON MAX_INPUT MB_LEN_MAX NAME_MAX NGROUPS_MAX OPEN_MAX PATH_MAX PIPE_BUF SCHAR_MAX SCHAR_MIN SHRT_MAX SHRT_MIN SSIZE_MAX STREAM_MAX TZNAME_MAX UCHAR_MAX UINT_MAX ULONG_MAX USHRT_MAX</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="LOCALE"
>LOCALE <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>LC_ALL LC_COLLATE LC_CTYPE LC_MONETARY LC_NUMERIC LC_TIME</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="MATH"
>MATH <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>HUGE_VAL</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="SIGNAL"
>SIGNAL <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>SA_NOCLDSTOP SA_NOCLDWAIT SA_NODEFER SA_ONSTACK SA_RESETHAND SA_RESTART SA_SIGINFO SIGABRT SIGALRM SIGCHLD SIGCONT SIGFPE SIGHUP SIGILL SIGINT SIGKILL SIGPIPE SIGQUIT SIGSEGV SIGSTOP SIGTERM SIGTSTP SIGTTIN SIGTTOU SIGUSR1 SIGUSR2 SIG_BLOCK SIG_DFL SIG_ERR SIG_IGN SIG_SETMASK SIG_UNBLOCK</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="STAT"
>STAT <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>S_IRGRP S_IROTH S_IRUSR S_IRWXG S_IRWXO S_IRWXU S_ISGID S_ISUID S_IWGRP S_IWOTH S_IWUSR S_IXGRP S_IXOTH S_IXUSR</p>

<dt><a name="Macros"
>Macros</a></dt>

<dd>
<p>S_ISBLK S_ISCHR S_ISDIR S_ISFIFO S_ISREG</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="STDLIB"
>STDLIB <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>EXIT_FAILURE EXIT_SUCCESS MB_CUR_MAX RAND_MAX</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="STDIO"
>STDIO <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>BUFSIZ EOF FILENAME_MAX L_ctermid L_cuserid L_tmpname TMP_MAX</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="TIME"
>TIME <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>CLK_TCK CLOCKS_PER_SEC</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="UNISTD"
>UNISTD <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>R_OK SEEK_CUR SEEK_END SEEK_SET STDIN_FILENO STDOUT_FILENO STDERR_FILENO W_OK X_OK</p>
</dd>
</dl>

<h1><a class='u' href='#___top' title='click to go to top of document'
name="WAIT"
>WAIT <img alt='^' src='http://st.pimg.net/tucs/img/up.gif'></a></h1>

<dl>
<dt><a name="Constants"
>Constants</a></dt>

<dd>
<p>WNOHANG WUNTRACED</p>

<dl>
<dt><a name="WNOHANG"
>WNOHANG</a></dt>

<dd>
<p>Do not suspend the calling process until a child process changes state but instead return immediately.</p>

<dt><a name="WUNTRACED"
>WUNTRACED</a></dt>

<dd>
<p>Catch stopped child processes.</p>
</dd>
</dl>

<dt><a name="Macros"
>Macros</a></dt>

<dd>
<p>WIFEXITED WEXITSTATUS WIFSIGNALED WTERMSIG WIFSTOPPED WSTOPSIG</p>

<dl>
<dt><a name="WIFEXITED"
>WIFEXITED</a></dt>

<dd>
<p>WIFEXITED(${^CHILD_ERROR_NATIVE}) returns true if the child process exited normally (<code>exit()</code> or by falling off the end of <code>main()</code>)</p>

<dt><a name="WEXITSTATUS"
>WEXITSTATUS</a></dt>

<dd>
<p>WEXITSTATUS(${^CHILD_ERROR_NATIVE}) returns the normal exit status of the child process (only meaningful if WIFEXITED(${^CHILD_ERROR_NATIVE}) is true)</p>

<dt><a name="WIFSIGNALED"
>WIFSIGNALED</a></dt>

<dd>
<p>WIFSIGNALED(${^CHILD_ERROR_NATIVE}) returns true if the child process terminated because of a signal</p>

<dt><a name="WTERMSIG"
>WTERMSIG</a></dt>

<dd>
<p>WTERMSIG(${^CHILD_ERROR_NATIVE}) returns the signal the child process terminated for (only meaningful if WIFSIGNALED(${^CHILD_ERROR_NATIVE}) is true)</p>

<dt><a name="WIFSTOPPED"
>WIFSTOPPED</a></dt>

<dd>
<p>WIFSTOPPED(${^CHILD_ERROR_NATIVE}) returns true if the child process is currently stopped (can happen only if you specified the WUNTRACED flag to waitpid())</p>

<dt><a name="WSTOPSIG"
>WSTOPSIG</a></dt>

<dd>
<p>WSTOPSIG(${^CHILD_ERROR_NATIVE}) returns the signal the child process was stopped for (only meaningful if WIFSTOPPED(${^CHILD_ERROR_NATIVE}) is true)</p>
</dd>
</dl>
</dd>
</dl>

</div>
<!-- This should probably be put in the <div class="footer"></div> -->
<script type="text/javascript">
    $(document).ready(function(){
        var startingStyle = $.cookie('css') ? $.cookie('css') : 'http://st.pimg.net/tucs/css/sh_none.min.css';
        $.fn.styleSwitch(startingStyle);
        $("#styleswitch").val(startingStyle);
        sh_highlightDocument();
        $("#styleswitch").bind(($.browser.msie ? "click" : "change"), function() {
            $.fn.styleSwitch($(this).val());
        });
    });
</script>
<div class="styleswitch">
    syntax highlighting:
    <select id="styleswitch">
        <option value="http://st.pimg.net/tucs/css/sh_none.min.css">no syntax highlighting</option>
        <option value="http://st.pimg.net/tucs/css/sh_acid.min.css">acid</option>
        <option value="http://st.pimg.net/tucs/css/sh_berries-dark.min.css">berries-dark</option>
        <option value="http://st.pimg.net/tucs/css/sh_berries-light.min.css">berries-light</option>
        <option value="http://st.pimg.net/tucs/css/sh_bipolar.min.css">bipolar</option>
        <option value="http://st.pimg.net/tucs/css/sh_blacknblue.min.css">blacknblue</option>
        <option value="http://st.pimg.net/tucs/css/sh_bright.min.css">bright</option>
        <option value="http://st.pimg.net/tucs/css/sh_contrast.min.css">contrast</option>
        <option value="http://st.pimg.net/tucs/css/sh_cpan.min.css">cpan</option>
        <option value="http://st.pimg.net/tucs/css/sh_darkblue.min.css">darkblue</option>
        <option value="http://st.pimg.net/tucs/css/sh_darkness.min.css">darkness</option>
        <option value="http://st.pimg.net/tucs/css/sh_desert.min.css">desert</option>
        <option value="http://st.pimg.net/tucs/css/sh_dull.min.css">dull</option>
        <option value="http://st.pimg.net/tucs/css/sh_easter.min.css">easter</option>
        <option value="http://st.pimg.net/tucs/css/sh_emacs.min.css">emacs</option>
        <option value="http://st.pimg.net/tucs/css/sh_golden.min.css">golden</option>
        <option value="http://st.pimg.net/tucs/css/sh_greenlcd.min.css">greenlcd</option>
        <option value="http://st.pimg.net/tucs/css/sh_ide-anjuta.min.css">ide-anjuta</option>
        <option value="http://st.pimg.net/tucs/css/sh_ide-codewarrior.min.css">ide-codewarrior</option>
        <option value="http://st.pimg.net/tucs/css/sh_ide-devcpp.min.css">ide-devcpp</option>
        <option value="http://st.pimg.net/tucs/css/sh_ide-eclipse.min.css">ide-eclipse</option>
        <option value="http://st.pimg.net/tucs/css/sh_ide-kdev.min.css">ide-kdev</option>
        <option value="http://st.pimg.net/tucs/css/sh_ide-msvcpp.min.css">ide-msvcpp</option>
        <option value="http://st.pimg.net/tucs/css/sh_kwrite.min.css">kwrite</option>
        <option value="http://st.pimg.net/tucs/css/sh_matlab.min.css">matlab</option>
        <option value="http://st.pimg.net/tucs/css/sh_navy.min.css">navy</option>
        <option value="http://st.pimg.net/tucs/css/sh_nedit.min.css">nedit</option>
        <option value="http://st.pimg.net/tucs/css/sh_neon.min.css">neon</option>
        <option value="http://st.pimg.net/tucs/css/sh_night.min.css">night</option>
        <option value="http://st.pimg.net/tucs/css/sh_pablo.min.css">pablo</option>
        <option value="http://st.pimg.net/tucs/css/sh_peachpuff.min.css">peachpuff</option>
        <option value="http://st.pimg.net/tucs/css/sh_print.min.css">print</option>
        <option value="http://st.pimg.net/tucs/css/sh_rand01.min.css">rand01</option>
	<option value="http://st.pimg.net/tucs/css/sh_solarized-dark.min.css">solarized-dark</option>
	<option value="http://st.pimg.net/tucs/css/sh_solarized-light.min.css">solarized-light</option>
        <option value="http://st.pimg.net/tucs/css/sh_style.min.css">style</option>
        <option value="http://st.pimg.net/tucs/css/sh_the.min.css">the</option>
        <option value="http://st.pimg.net/tucs/css/sh_typical.min.css">typical</option>
        <option value="http://st.pimg.net/tucs/css/sh_vampire.min.css">vampire</option>
        <option value="http://st.pimg.net/tucs/css/sh_vim-dark.min.css">vim-dark</option>
        <option value="http://st.pimg.net/tucs/css/sh_vim.min.css">vim</option>
        <option value="http://st.pimg.net/tucs/css/sh_whatis.min.css">whatis</option>
        <option value="http://st.pimg.net/tucs/css/sh_whitengrey.min.css">whitengrey</option>
        <option value="http://st.pimg.net/tucs/css/sh_zellner.min.css">zellner</option>
    </select>
</div>



<div class="footer"><div class="cpanstats">78252 Uploads, 24722 Distributions
106026 Modules, 9649 Uploaders
</div>
hosted by <a href="http://www.yellowbot.com">YellowBot</a><br/>
<a href="http://www.yellowbot.com"><img alt="do. tag. write. share." src="http://st.pimg.net/tucs/img/yellowbot_logo.gif"></a>
</div>
<script type="text/javascript">
var gaJsHost = (("https:" == document.location.protocol) ? "https://ssl." : "http://www.");
document.write(unescape("%3Cscript src='" + gaJsHost + "google-analytics.com/ga.js' type='text/javascript'%3E%3C/script%3E"));
</script>
<script type="text/javascript" src="http://ipv4.v6test.develooper.com/js/v1/v6test.js"></script>

<script type="text/javascript">
   // v6.target = '';
   if (!v6.target) { v6.only_once = true }
   v6.site = '7A0D89A6-2B82-11DF-B9DA-F61CBD13F020';
   v6.api_server = 'http://ipv4.v6test.develooper.com';
   try {
     v6.test();
   } catch(err) {}
</script>
<script type="text/javascript">
  $(document).ready(function(){
    $("a[href^=http:]").click(function(){
      var href = $(this).attr('href');
      var m = href.match('\/\/([^\/:]+)');
      _gaq.push(['_trackEvent','External',m[1],'Module']);
    });
    $("a[href^=/CPAN/]").click(function(){
      var href = $(this).attr('href');
      _gaq.push(['_trackEvent','Download',href,'Module']);
    });
  });
</script>
<!-- Thu Apr 26 17:13:50 2012 GMT (0.286740064620972) @cpansearch1 -->
 </body>
</html>
